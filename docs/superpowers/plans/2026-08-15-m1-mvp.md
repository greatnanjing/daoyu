# 刀鱼 M1 (MVP) 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 打通"微信消息 → SQLite 落盘 → worker 调 `claude -p` headless 子进程 → 流式解析 → 回推微信"全链路，含统一命令总线与崩溃恢复。

**Architecture:** 单进程 asyncio 应用：gateway（iLink 长轮询收发 + 命令路由）与 worker（任务池调 claude 子进程）共享一个 WAL 模式 SQLite 作为唯一事实源。一切先落盘：入站消息、任务、出站全部进 DB，进程任意时刻崩溃可恢复。

**Tech Stack:** Python 3.11+（开发机 3.12.10）、aiohttp（iLink HTTP）、sqlite3（标准库）、pytest + pytest-asyncio + aioresponses（测试）。

**权威文档:** [docs/PRD.md](../../PRD.md)、[docs/TRD.md](../../TRD.md)。冲突时以 TRD 为准，本计划补充实现细节。

## Global Constraints

以下全部为**已实测核实**的事实（2026-08-15，来源：SiverKing/weixin-ClawBot-API 源码与文档、本机 `claude --help`），违反即 bug：

### iLink 协议（ClawBot）

1. 域名 `https://ilinkai.weixin.qq.com`，纯 HTTP/JSON。
2. 每次请求 headers：`Content-Type: application/json`、`AuthorizationType: ilink_bot_token`、`X-WECHAT-UIN` = base64(str(random_uint32))（**每次随机**）、`iLink-App-Id: bot`、`iLink-App-ClientVersion: 132099`（= `(2<<16)|(4<<8)|3` 即 2.4.3）、登录后加 `Authorization: Bearer <bot_token>`。
3. 请求体带 `base_info`: `{"channel_version": "2.4.3", "bot_agent": "daoyu/0.1.0 (python)"}`（sendtyping 也要带）。
4. **aiohttp 解析响应必须 `content_type=None`**（服务器返回 `application/octet-stream`）。
5. `getupdates`：POST `{"get_updates_buf": <游标>, "base_info": ...}`，服务器 hold 35s；响应 `{msgs: [], get_updates_buf, longpolling_timeout_ms: 35000, errcode: 0}`。游标必须每次更新否则重收消息。
6. 入站消息字段：`message_id`（int，去重键）、`seq`、`from_user_id`（`xxx@im.wechat`）、`to_user_id`（`xxx@im.bot`）、`message_type`（1=文本）、`context_token`、`item_list[].text_item.text`；可能带 `group_id`（群消息，**忽略不回**）。
7. `sendmessage` 字段必须**全填**，缺任何一项会 HTTP 200 但静默不投递：
   ```json
   {"msg": {"from_user_id": "", "to_user_id": "...", "client_id": "daoyu-<8hex>",
            "message_type": 2, "message_state": 2, "context_token": "...",
            "item_list": [{"type": 1, "text_item": {"text": "..."}}]},
    "base_info": {...}}
   ```
   成功响应为 HTTP 200（响应体可能为空 `{}`）；`errcode` 非 0 视为失败。
8. `context_token` 只用**当前会话最新入站消息**的，绝不复用历史值（复用旧值 200 但不投递）。
9. `getconfig`：POST `{"ilink_user_id", "context_token", "base_info"}` → `typing_ticket`，每用户缓存 24h，重连后清空重取。
10. `sendtyping`：POST `{"ilink_user_id", "typing_ticket", "status": 1|2, "base_info"}`（1=正在输入，2=结束）。
11. 登录：POST `ilink/bot/get_bot_qrcode?bot_type=3` body `{"local_token_list": [<旧token>]}` → `{qrcode, qrcode_img_content}`（img_content 是 URL 不是图片数据）；GET 失败可回退旧版 GET。轮询 GET `ilink/bot/get_qrcode_status?qrcode=<urlencoded>`，status ∈ `wait/scanned/confirmed/expired/binded_redirect/scaned_but_redirect/need_verifycode/verify_code_blocked`；`confirmed` → `{bot_token, baseurl, ...}`；`scaned_but_redirect` → 切换 `redirect_host` 为新 base。
12. 连接 24h 过期，每次扫码 Bot ID 变化，重连后 token 原子替换、typing_ticket 缓存清空。

### claude CLI（本机 `claude --help` 核实）

13. `--bare` 存在：跳过 hooks/LSP/插件/CLAUDE.md 自动发现/自动记忆；**认证只认 `ANTHROPIC_API_KEY` 环境变量或 `--settings` 的 apiKeyHelper**（OAuth/keychain 永不读）。
14. `--permission-mode` choices：`acceptEdits | auto | bypassPermissions | manual | dontAsk | plan`（比 TRD 设想多 `auto`/`manual`/`dontAsk`；M1 按 TRD 映射用 acceptEdits/bypassPermissions/plan，`auto` 模式行为 M1 实测后再定）。
15. `--max-budget-usd`、`--max-turns`（仅 `--print` 下）、`--output-format stream-json --verbose --include-partial-messages`、`--allowedTools`/`--disallowedTools`、`--session-id <uuid>`、`--resume <uuid>`、`--mcp-config`、`--strict-mcp-config`、`--settings` 均存在。
16. **`--permission-prompt-tool` 已不存在于当前 CLI**（M2 审批依赖，开放问题）；**`claude logs` 子命令不存在**，后台任务管理是 `claude agents --json`（M2 长任务依赖，开放问题）。均不影响 M1。
17. 每次调用**全量传 flag**：`--resume` 不恢复 permission-mode/mcp-config/add-dir。
18. prompt 经 **stdin** 传入 `claude -p`；子进程 **cwd = 会话绑定工作目录**；同一 session UUID 任务必须串行。

### 工程约束

19. 源码目录结构按 TRD §7：`gateway/ worker/ common/ claude/ deploy/ data/`；`data/daoyu.db`、`gateway/config.json`、`claude/secrets.env` 进 gitignore。
20. 开发在 Windows（Python 3.12.10，Git Bash），部署在 Linux。所有路径用 `pathlib`，不写死分隔符；venv 用 `python -m venv` + `python -m pip`（本机 PATH 中 pip 指向另一个 conda Python，必须显式 `-m`）。
21. 每个任务 TDD：先写测试、跑失败、实现、跑过、commit。commit 信息用 conventional 格式（`feat:`/`test:`/`chore:`）。
22. 微信文本分页阈值未实测（TRD §11 开放问题），M1 用 2000 字符/条（可配置），实测后调。

---

## File Structure

```
daoyu/
├── pyproject.toml              # 包元数据 + pytest 配置 + 依赖
├── .gitignore
├── README.md                   # 运行/部署说明
├── common/
│   ├── __init__.py
│   ├── models.py               # dataclass: InboundMessage/SessionBinding/Task/OutboxItem/Budget
│   ├── config.py               # 配置加载 (gateway/config.json + claude/secrets.env)
│   └── db.py                   # Database: schema + 全部 CRUD（唯一事实源）
├── gateway/
│   ├── __init__.py
│   ├── ilink.py                # ILinkClient: 纯 iLink 协议封装，无业务逻辑
│   ├── router.py               # route(): 统一命令路由纯函数
│   ├── bridge.py               # 桥命令 + iLink 运维命令 + /help 生成
│   ├── outbound.py             # OutboundLoop: outbox→iLink 发送（节流/分页/typing/重试/死信）
│   ├── reconnect.py            # 24h 重连定时器 + 终端登录
│   ├── login.py                # 终端扫码登录（写 token 进 DB）
│   ├── app.py                  # 主入口：组装一切 + 入站管道
│   └── config.example.json     # 配置样例（进 git；config.json 实例 gitignore）
├── worker/
│   ├── __init__.py
│   ├── cli_builder.py          # build_argv(): policy/预算 → claude argv
│   ├── stream.py               # StreamParser: NDJSON 事件解析 + Throttle 节流器
│   ├── runner.py               # TaskRunner: 子进程执行单任务
│   └── pool.py                 # WorkerPool: 调度（session 串行/并行/恢复/取消）
├── tests/
│   ├── conftest.py             # tmp DB fixture / fake_claude fixture
│   ├── fixtures/fake_claude.py # 回放 stream-json 的假 claude CLI
│   ├── fixtures/review_stream.jsonl
│   └── test_*.py
├── claude/
│   ├── settings.json           # 刀鱼 Claude 实例持久配置（进 git）
│   ├── mcp.json                # MCP 清单，M1 为 {}（进 git）
│   └── secrets.env.example     # ANTHROPIC_API_KEY=...（实例 gitignore）
├── deploy/
│   └── daoyu.service           # systemd 单元
└── data/                       # 运行时 SQLite（gitignore）
```

**跨任务接口契约**（后续任务按此实现，签名不得偏离）：

```python
# common/models.py
@dataclass InboundMessage: msg_id: str; from_user: str; text: str; context_token: str; received_at: int; id: int | None = None
@dataclass SessionBinding: id: int; wechat_user: str; cwd: str; claude_uuid: str; policy: str; created_at: int; last_active_at: int
@dataclass Task: id: int; message_id: int | None; session_id: int; prompt: str; kind: str; state: str; attempts: int; max_attempts: int; created_at: int; updated_at: int
@dataclass OutboxItem: id: int; task_id: int | None; to_user: str; text: str; seq: int; state: str; attempts: int; max_attempts: int; last_error: str | None; created_at: int
@dataclass Budget: max_turns: int = 50; max_usd: float = 5.0

# common/db.py — Database（全部方法，见 Task 2-4）
# gateway/ilink.py — ILinkClient（见 Task 5）
# gateway/router.py — Route = namedtuple/dataclass(kind, command, args)；route(text, slash_commands: set[str]) -> Route
# worker/cli_builder.py — build_argv(*, session_uuid, resume, policy, budget, mcp_config, settings) -> list[str]
# worker/stream.py — StreamParser.feed_line(line) -> StreamEvent | None；Throttle.allow(now) -> bool
# worker/runner.py — TaskRunner(db, config, process_registry).run(task, session) -> None
# worker/pool.py — WorkerPool(db, config).run_forever() / .cancel(task_id) -> str / .running_session_ids -> set[int]
# gateway/outbound.py — OutboundLoop(db, ilink, config).run_forever()；split_text(text, limit) -> list[str]
# gateway/bridge.py — async execute_bridge(db, pool, route, from_user) -> str
```

---

### Task 1: 项目骨架

**Files:**
- Create: `pyproject.toml`、`.gitignore`、`common/__init__.py`、`gateway/__init__.py`、`worker/__init__.py`、`tests/conftest.py`、`tests/test_smoke.py`、`claude/settings.json`、`claude/mcp.json`、`claude/secrets.env.example`、`gateway/config.example.json`、`deploy/daoyu.service`
- Test: `tests/test_smoke.py`

**Interfaces:**
- Produces: 可安装可测试的空项目；`common`/`gateway`/`worker` 三个可导入包。

- [ ] **Step 1: 初始化 git 仓库与目录**

```bash
cd /d/Sync/github/daoyu
git init
mkdir -p common gateway worker tests/fixtures claude deploy data
```

- [ ] **Step 2: 写 pyproject.toml**

```toml
[project]
name = "daoyu"
version = "0.1.0"
description = "把微信变成 Claude Code 的遥控器"
requires-python = ">=3.11"
dependencies = ["aiohttp>=3.9"]

[project.scripts]
daoyu = "gateway.app:start"
daoyu-login = "gateway.login:main"

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-asyncio>=0.23", "aioresponses>=0.7"]
qr = ["qrcode[pil]>=7"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["common", "gateway", "worker"]
```

- [ ] **Step 3: 写 .gitignore**

```gitignore
__pycache__/
*.pyc
.venv/
data/
gateway/config.json
claude/secrets.env
.pytest_cache/
qrcode.*
```

- [ ] **Step 4: 建包与配置文件**

`common/__init__.py`、`gateway/__init__.py`、`worker/__init__.py` 均为空文件。

`claude/settings.json`：
```json
{
  "permissions": {
    "deny": [
      "Read(/etc/**)", "Read(~/.ssh/**)", "Read(~/.claude/**)",
      "Edit(/)", "Edit(/etc/**)", "Edit(~/.ssh/**)", "Edit(~/.claude/**)",
      "Edit(./data/daoyu.db)", "Bash(rm -rf /*)", "Bash(rm -rf ~)"
    ]
  }
}
```

`claude/mcp.json`：
```json
{}
```

`claude/secrets.env.example`：
```
ANTHROPIC_API_KEY=sk-ant-xxxx
```

`gateway/config.example.json`：
```json
{
  "whitelist": ["your-wechat-user-id@im.wechat"],
  "default_cwd": "/home/<user>/proj/daoyu",
  "claude_bin": "claude",
  "throttle": {"min_send_interval_s": 1.0, "progress_window_s": 2.5, "page_char_limit": 2000, "daily_send_limit": 500},
  "budget": {"max_turns": 50, "max_usd": 5.0},
  "worker": {"concurrency": 3, "poll_interval_s": 0.5},
  "reconnect": {"session_duration_s": 86400, "warning_before_s": 7200, "reminder_interval_s": 1800, "force_before_s": 1800, "qrcode_scan_timeout_s": 600}
}
```

`deploy/daoyu.service`：
```ini
[Unit]
Description=daoyu - WeChat remote for Claude Code
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=<user>
WorkingDirectory=/home/<user>/proj/daoyu
ExecStart=/home/<user>/proj/daoyu/.venv/bin/daoyu
Restart=always
RestartSec=5
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 5: 写共享 fixture `tests/conftest.py` 与冒烟测试 `tests/test_smoke.py`**

```python
# tests/conftest.py
import pytest

from common.db import Database


@pytest.fixture
def db(tmp_path):
    """所有测试共享的临时数据库（WAL）。"""
    d = Database(tmp_path / "test.db")
    d.ensure_schema()
    return d
```

```python
# tests/test_smoke.py
def test_packages_importable():
    import common
    import gateway
    import worker


def test_claude_config_files_exist():
    from pathlib import Path
    root = Path(__file__).resolve().parents[1]
    assert (root / "claude" / "settings.json").is_file()
    assert (root / "claude" / "mcp.json").is_file()
    assert (root / "gateway" / "config.example.json").is_file()
```


- [ ] **Step 6: 建 venv、安装依赖、跑测试**

```bash
python -m venv .venv
. .venv/Scripts/activate   # Linux: source .venv/bin/activate
python -m pip install -e ".[dev]"
python -m pytest -v
```

Expected: 2 passed

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "chore: 项目骨架（包结构/配置样例/systemd/pytest）"
```

---

### Task 2: common/models.py + db.py — schema 与 state KV

**Files:**
- Create: `common/models.py`、`common/db.py`
- Test: `tests/test_db.py`

**Interfaces:**
- Produces: `Database(path)`（`.ensure_schema()`、`.get_state(key, default=None)`、`.set_state(key, value)`、`.audit(kind, detail)`、`.today_cost_usd()`、`.queue_depth()`）；models 全部 dataclass。

- [ ] **Step 1: 写 models `common/models.py`**

```python
"""数据模型：与 TRD §6 五表一一对应，外加运行时 state KV（不在 TRD 内，为'一切先落盘'所需）。"""
from dataclasses import dataclass


@dataclass
class InboundMessage:
    msg_id: str
    from_user: str
    text: str
    context_token: str
    received_at: int
    id: int | None = None


@dataclass
class SessionBinding:
    id: int
    wechat_user: str
    cwd: str
    claude_uuid: str
    policy: str
    created_at: int
    last_active_at: int


@dataclass
class Task:
    id: int
    message_id: int | None
    session_id: int
    prompt: str
    kind: str          # chat / command
    state: str         # pending / running / done / failed / dead / canceled
    attempts: int
    max_attempts: int
    created_at: int
    updated_at: int


@dataclass
class OutboxItem:
    id: int
    task_id: int | None
    to_user: str
    text: str
    seq: int
    state: str         # pending / sent / failed / dead
    attempts: int
    max_attempts: int
    last_error: str | None
    created_at: int


@dataclass
class Budget:
    max_turns: int = 50
    max_usd: float = 5.0
```

- [ ] **Step 2: 写失败测试 `tests/test_db.py`（schema 部分）**

```python
import json

import pytest

from common.db import Database  # noqa: F401（conftest 的 db fixture 依赖）


def test_ensure_schema_idempotent(db, tmp_path):
    db.ensure_schema()  # 二次调用不报错
    names = {r[0] for r in db._conn.execute(
        "SELECT name FROM sqlite_master WHERE type='table'")}
    assert {"messages", "tasks", "outbox", "sessions", "audit_log", "state"} <= names


def test_wal_mode(db, tmp_path):
    mode = db._conn.execute("PRAGMA journal_mode").fetchone()[0]
    assert mode == "wal"


def test_state_kv_roundtrip(db):
    assert db.get_state("missing", "dft") == "dft"
    db.set_state("bot_token", "T1")
    assert db.get_state("bot_token") == "T1"
    db.set_state("bot_token", "T2")  # 覆盖
    assert db.get_state("bot_token") == "T2"


def test_audit_and_cost(db):
    db.audit("cost", json.dumps({"task_id": 1, "usd": 0.42}))
    assert db.today_cost_usd() == pytest.approx(0.42)
    db.audit("cost", json.dumps({"task_id": 2, "usd": 0.08}))
    assert db.today_cost_usd() == pytest.approx(0.50)
```

- [ ] **Step 3: 跑测试确认失败**

Run: `python -m pytest tests/test_db.py -v`
Expected: FAIL（`ModuleNotFoundError: common.db`）

- [ ] **Step 4: 实现 `common/db.py`（schema + state + audit）**

```python
"""SQLite 唯一事实源。所有访问都在事件循环线程同步执行（WAL 下微秒级，M1 并发 2~3 可接受）。"""
import json
import sqlite3
import time
from pathlib import Path

_SCHEMA = """
CREATE TABLE IF NOT EXISTS sessions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  wechat_user TEXT NOT NULL,
  cwd TEXT NOT NULL,
  claude_uuid TEXT NOT NULL,
  policy TEXT NOT NULL DEFAULT 'auto',
  created_at INTEGER NOT NULL,
  last_active_at INTEGER NOT NULL,
  UNIQUE(wechat_user, cwd)
);
CREATE TABLE IF NOT EXISTS messages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  msg_id TEXT UNIQUE NOT NULL,
  from_user TEXT NOT NULL,
  text TEXT NOT NULL DEFAULT '',
  context_token TEXT NOT NULL DEFAULT '',
  received_at INTEGER NOT NULL,
  state TEXT NOT NULL DEFAULT 'received'
);
CREATE TABLE IF NOT EXISTS tasks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  message_id INTEGER REFERENCES messages(id),
  session_id INTEGER NOT NULL REFERENCES sessions(id),
  prompt TEXT NOT NULL,
  kind TEXT NOT NULL DEFAULT 'chat',
  state TEXT NOT NULL DEFAULT 'pending',
  attempts INTEGER NOT NULL DEFAULT 0,
  max_attempts INTEGER NOT NULL DEFAULT 3,
  claude_bg_id TEXT,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_tasks_state ON tasks(state, created_at);
CREATE TABLE IF NOT EXISTS outbox (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  task_id INTEGER REFERENCES tasks(id),
  to_user TEXT NOT NULL,
  text TEXT NOT NULL,
  seq INTEGER NOT NULL DEFAULT 0,
  state TEXT NOT NULL DEFAULT 'pending',
  attempts INTEGER NOT NULL DEFAULT 0,
  max_attempts INTEGER NOT NULL DEFAULT 5,
  last_error TEXT,
  created_at INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_outbox_state ON outbox(state, id);
CREATE TABLE IF NOT EXISTS audit_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  ts INTEGER NOT NULL,
  kind TEXT NOT NULL,
  detail TEXT NOT NULL DEFAULT ''
);
CREATE TABLE IF NOT EXISTS state (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL,
  updated_at INTEGER NOT NULL
);
"""


class Database:
    def __init__(self, path: str | Path):
        self._conn = sqlite3.connect(str(path), check_same_thread=False)
        self._conn.row_factory = sqlite3.Row

    def ensure_schema(self) -> None:
        self._conn.execute("PRAGMA journal_mode=WAL")
        self._conn.execute("PRAGMA foreign_keys=ON")
        self._conn.executescript(_SCHEMA)
        self._conn.commit()

    # ---- state KV（bot_token / get_updates_buf / slash_commands / cwd 指针等）----
    def get_state(self, key: str, default: str | None = None) -> str | None:
        row = self._conn.execute("SELECT value FROM state WHERE key=?", (key,)).fetchone()
        return row["value"] if row else default

    def set_state(self, key: str, value: str) -> None:
        self._conn.execute(
            "INSERT INTO state(key, value, updated_at) VALUES(?,?,?) "
            "ON CONFLICT(key) DO UPDATE SET value=excluded.value, updated_at=excluded.updated_at",
            (key, value, int(time.time())))
        self._conn.commit()

    # ---- audit ----
    def audit(self, kind: str, detail: str) -> None:
        self._conn.execute(
            "INSERT INTO audit_log(ts, kind, detail) VALUES(?,?,?)",
            (int(time.time()), kind, detail))
        self._conn.commit()

    def today_cost_usd(self) -> float:
        day_start = int(time.time()) - int(time.time()) % 86400
        total = 0.0
        for row in self._conn.execute(
                "SELECT detail FROM audit_log WHERE kind='cost' AND ts>=?", (day_start,)):
            try:
                total += float(json.loads(row["detail"]).get("usd", 0))
            except (ValueError, TypeError):
                pass
        return total

    def queue_depth(self) -> int:
        return self._conn.execute(
            "SELECT COUNT(*) c FROM tasks WHERE state='pending'").fetchone()["c"]
```

- [ ] **Step 5: 跑测试确认通过**

Run: `python -m pytest tests/test_db.py -v`
Expected: 5 passed

- [ ] **Step 6: Commit**

```bash
git add common/ tests/ && git commit -m "feat: SQLite schema（TRD 五表+state KV）与审计记账"
```

---

### Task 3: db — messages 去重 + sessions 会话绑定

**Files:**
- Modify: `common/db.py`（追加方法）
- Test: `tests/test_db.py`（追加）

**Interfaces:**
- Consumes: Task 2 的 Database。
- Produces:
  - `insert_message(msg: InboundMessage) -> int | None`（重复返回 None）
  - `latest_context_token(from_user) -> str | None`
  - `get_or_create_session(wechat_user, cwd) -> SessionBinding`（claude_uuid 预生成）
  - `set_policy(session_id, policy) -> None`
  - `get_session(session_id) -> SessionBinding | None`
  - `set_active_cwd(wechat_user, cwd)` / `get_active_cwd(wechat_user, default) -> str`
  - `list_sessions(wechat_user) -> list[SessionBinding]`

- [ ] **Step 1: 追加失败测试**

```python
# tests/test_db.py 追加
from common.models import InboundMessage


def _msg(n, token="tokA"):
    return InboundMessage(msg_id=str(n), from_user="u@im.wechat", text=f"hi{n}",
                          context_token=token, received_at=1000)


def test_insert_message_dedup(db):
    assert db.insert_message(_msg(1)) is not None      # 新消息 → id
    assert db.insert_message(_msg(1)) is None          # 同 msg_id → None（去重）
    assert db.insert_message(_msg(2)) is not None


def test_latest_context_token(db):
    db.insert_message(_msg(1, token="t1"))
    db.insert_message(_msg(2, token="t2"))
    assert db.latest_context_token("u@im.wechat") == "t2"


def test_get_or_create_session(db):
    s1 = db.get_or_create_session("u@im.wechat", "/repo")
    s2 = db.get_or_create_session("u@im.wechat", "/repo")   # 幂等
    assert s1.id == s2.id and s1.claude_uuid == s2.claude_uuid
    s3 = db.get_or_create_session("u@im.wechat", "/other")  # 不同 cwd → 新会话
    assert s3.id != s1.id and s3.claude_uuid != s1.claude_uuid
    assert s1.policy == "auto"

    db.set_policy(s1.id, "strict")
    assert db.get_session(s1.id).policy == "strict"
    assert len(db.list_sessions("u@im.wechat")) == 2


def test_active_cwd_pointer(db):
    db.set_active_cwd("u@im.wechat", "/repo")
    assert db.get_active_cwd("u@im.wechat", "/dft") == "/repo"
    assert db.get_active_cwd("other@im.wechat", "/dft") == "/dft"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_db.py -v -k "dedup or session or cwd or token"`
Expected: FAIL（AttributeError: insert_message）

- [ ] **Step 3: 实现（追加到 Database 类）**

```python
    # ---- messages ----
    def insert_message(self, msg: InboundMessage) -> int | None:
        cur = self._conn.execute(
            "INSERT OR IGNORE INTO messages(msg_id, from_user, text, context_token, received_at) "
            "VALUES(?,?,?,?,?)",
            (msg.msg_id, msg.from_user, msg.text, msg.context_token, msg.received_at))
        self._conn.commit()
        return cur.lastrowid if cur.rowcount else None

    def latest_context_token(self, from_user: str) -> str | None:
        row = self._conn.execute(
            "SELECT context_token FROM messages WHERE from_user=? "
            "ORDER BY id DESC LIMIT 1", (from_user,)).fetchone()
        return row["context_token"] if row else None

    # ---- sessions ----
    def get_or_create_session(self, wechat_user: str, cwd: str) -> SessionBinding:
        row = self._conn.execute(
            "SELECT * FROM sessions WHERE wechat_user=? AND cwd=?",
            (wechat_user, cwd)).fetchone()
        if row is None:
            import uuid
            now = int(time.time())
            self._conn.execute(
                "INSERT INTO sessions(wechat_user, cwd, claude_uuid, policy, created_at, last_active_at) "
                "VALUES(?,?,?,?,?,?)",
                (wechat_user, cwd, str(uuid.uuid4()), "auto", now, now))
            self._conn.commit()
            row = self._conn.execute(
                "SELECT * FROM sessions WHERE wechat_user=? AND cwd=?",
                (wechat_user, cwd)).fetchone()
        return SessionBinding(**dict(row))

    def get_session(self, session_id: int) -> SessionBinding | None:
        row = self._conn.execute("SELECT * FROM sessions WHERE id=?", (session_id,)).fetchone()
        return SessionBinding(**dict(row)) if row else None

    def set_policy(self, session_id: int, policy: str) -> None:
        self._conn.execute("UPDATE sessions SET policy=? WHERE id=?", (policy, session_id))
        self._conn.commit()

    def touch_session(self, session_id: int) -> None:
        self._conn.execute(
            "UPDATE sessions SET last_active_at=? WHERE id=?",
            (int(time.time()), session_id))
        self._conn.commit()

    def list_sessions(self, wechat_user: str) -> list[SessionBinding]:
        rows = self._conn.execute(
            "SELECT * FROM sessions WHERE wechat_user=? ORDER BY last_active_at DESC",
            (wechat_user,)).fetchall()
        return [SessionBinding(**dict(r)) for r in rows]

    # ---- 每用户当前 cwd 指针（state KV）----
    def set_active_cwd(self, wechat_user: str, cwd: str) -> None:
        self.set_state(f"cwd:{wechat_user}", cwd)

    def get_active_cwd(self, wechat_user: str, default: str) -> str:
        return self.get_state(f"cwd:{wechat_user}", default) or default
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_db.py -v`
Expected: 全部 passed

- [ ] **Step 5: Commit**

```bash
git add common/ tests/ && git commit -m "feat: 入站消息 msg_id 幂等去重 + (用户,cwd)→Claude 会话绑定"
```

---

### Task 4: db — tasks / outbox 状态机

**Files:**
- Modify: `common/db.py`（追加方法）
- Test: `tests/test_db.py`（追加）

**Interfaces:**
- Produces:
  - `create_task(message_id, session_id, prompt, kind='chat', max_attempts=3) -> int`
  - `claim_next_pending(session_ids: set[int]) -> Task | None`（pending→running，原子）
  - `pending_sessions() -> list[int]`（有 pending 任务的 session id，FIFO 序）
  - `finish_task(task_id, state)`（done/failed/canceled；failed 时 attempts 已在 claim 时 +1，未超限则回 pending）
  - `reset_running_tasks() -> int`（崩溃恢复：running→pending）
  - `active_tasks() -> list[Task]`、`get_task(task_id) -> Task | None`、`cancel_task(task_id) -> bool`（仅 pending 可直接取消）
  - `enqueue(task_id, to_user, text) -> int`、`next_outbox_batch(limit=10) -> list[OutboxItem]`（pending，发送前 attempts+1）
  - `mark_sent(outbox_id)`、`mark_send_failed(outbox_id, error)`（超 max_attempts → dead）
  - `dead_letter_count() -> int`、`retry_failed_outbox() -> int`（启动恢复：failed→pending）

- [ ] **Step 1: 追加失败测试**

```python
# tests/test_db.py 追加
def _session(db):
    return db.get_or_create_session("u@im.wechat", "/repo")


def test_task_lifecycle(db):
    s = _session(db)
    t1 = db.create_task(None, s.id, "/review", kind="command")
    t2 = db.create_task(None, s.id, "hello", kind="chat")
    assert db.pending_sessions() == [s.id]

    got = db.claim_next_pending({s.id})
    assert got.id == t1 and got.state == "running" and got.attempts == 1
    assert [t.id for t in db.active_tasks()] == [t1, t2]  # running+pending 都算活跃

    db.finish_task(t1, "done")
    got2 = db.claim_next_pending({s.id})
    assert got2.id == t2
    db.finish_task(t2, "failed")          # attempts=1 < max 3 → 回 pending 重试
    assert db.get_task(t2).state == "pending"


def test_task_retry_exhausted_to_dead(db):
    s = _session(db)
    t = db.create_task(None, s.id, "x", max_attempts=2)
    for _ in range(2):
        got = db.claim_next_pending({s.id})
        db.finish_task(t, "failed")
    assert db.get_task(t).state == "dead"


def test_reset_running_tasks_recovery(db):
    s = _session(db)
    db.create_task(None, s.id, "a")
    got = db.claim_next_pending({s.id})
    assert got.state == "running"
    n = db.reset_running_tasks()          # 模拟崩溃后重启
    assert n == 1 and db.get_task(got.id).state == "pending"


def test_cancel_pending_task(db):
    s = _session(db)
    t = db.create_task(None, s.id, "a")
    assert db.cancel_task(t) is True
    assert db.get_task(t).state == "canceled"
    assert db.claim_next_pending({s.id}) is None
    t2 = db.create_task(None, s.id, "b")
    db.claim_next_pending({s.id})         # running
    assert db.cancel_task(t2) is False    # running 的取消走 pool.kill，DB 不直接改


def test_outbox_state_machine(db):
    oid = db.enqueue(None, "u@im.wechat", "回复1")
    assert db.enqueue(None, "u@im.wechat", "回复2") == oid + 1
    batch = db.next_outbox_batch()
    assert [o.text for o in batch] == ["回复1", "回复2"]
    assert batch[0].attempts == 1

    db.mark_sent(batch[0].id)
    db.mark_send_failed(batch[1].id, "HTTP 500")
    assert db.get_outbox(batch[0].id).state == "sent"
    assert db.get_outbox(batch[1].id).state == "pending"   # 1 < 5 次，回 pending

    o = db.get_outbox(batch[1].id)
    for _ in range(4):                                     # 凑满 5 次
        b = db.next_outbox_batch()
        db.mark_send_failed(b[0].id, "again")
    assert db.get_outbox(batch[1].id).state == "dead"
    assert db.dead_letter_count() == 1


def test_retry_failed_outbox_on_startup(db):
    oid = db.enqueue(None, "u", "m")
    b = db.next_outbox_batch()
    db.mark_send_failed(b[0].id, "x")
    # failed→pending 转换在 mark_send_failed 内完成（未死信则回 pending），
    # 启动恢复只需重投 pending
    assert db.get_outbox(oid).state == "pending"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_db.py -v -k "task or outbox or cancel"`
Expected: FAIL

- [ ] **Step 3: 实现（追加到 Database 类）**

```python
    # ---- tasks ----
    def create_task(self, message_id: int | None, session_id: int, prompt: str,
                    kind: str = "chat", max_attempts: int = 3) -> int:
        now = int(time.time())
        cur = self._conn.execute(
            "INSERT INTO tasks(message_id, session_id, prompt, kind, state, "
            "max_attempts, created_at, updated_at) VALUES(?,?,?,?, 'pending',?,?,?)",
            (message_id, session_id, prompt, kind, max_attempts, now, now))
        self._conn.commit()
        return cur.lastrowid

    def _task_row(self, row) -> Task:
        return Task(id=row["id"], message_id=row["message_id"], session_id=row["session_id"],
                    prompt=row["prompt"], kind=row["kind"], state=row["state"],
                    attempts=row["attempts"], max_attempts=row["max_attempts"],
                    created_at=row["created_at"], updated_at=row["updated_at"])

    def get_task(self, task_id: int) -> Task | None:
        row = self._conn.execute("SELECT * FROM tasks WHERE id=?", (task_id,)).fetchone()
        return self._task_row(row) if row else None

    def pending_sessions(self) -> list[int]:
        rows = self._conn.execute(
            "SELECT DISTINCT session_id FROM tasks WHERE state='pending' ORDER BY MIN(id)").fetchall()
        return [r["session_id"] for r in rows]

    def claim_next_pending(self, session_ids: set[int]) -> Task | None:
        """pending→running（attempts+1）。一次事务，防并发双取。"""
        for sid in session_ids:
            cur = self._conn.execute(
                "UPDATE tasks SET state='running', attempts=attempts+1, updated_at=? "
                "WHERE id=(SELECT id FROM tasks WHERE state='pending' AND session_id=? "
                "ORDER BY id LIMIT 1) RETURNING *",
                (int(time.time()), sid))
            row = cur.fetchone()
            self._conn.commit()
            if row:
                return self._task_row(row)
        return None

    def finish_task(self, task_id: int, state: str) -> None:
        if state == "failed":
            row = self._conn.execute(
                "SELECT attempts, max_attempts FROM tasks WHERE id=?", (task_id,)).fetchone()
            if row and row["attempts"] < row["max_attempts"]:
                state = "pending"   # 未耗尽 → 回队列重试
        self._conn.execute(
            "UPDATE tasks SET state=?, updated_at=? WHERE id=?",
            (state, int(time.time()), task_id))
        self._conn.commit()

    def reset_running_tasks(self) -> int:
        cur = self._conn.execute(
            "UPDATE tasks SET state='pending', updated_at=? WHERE state='running'",
            (int(time.time()),))
        self._conn.commit()
        return cur.rowcount

    def active_tasks(self) -> list[Task]:
        rows = self._conn.execute(
            "SELECT * FROM tasks WHERE state IN ('running','pending') ORDER BY id").fetchall()
        return [self._task_row(r) for r in rows]

    def cancel_task(self, task_id: int) -> bool:
        cur = self._conn.execute(
            "UPDATE tasks SET state='canceled', updated_at=? WHERE id=? AND state='pending'",
            (int(time.time()), task_id))
        self._conn.commit()
        return bool(cur.rowcount)

    # ---- outbox ----
    def enqueue(self, task_id: int | None, to_user: str, text: str) -> int:
        cur = self._conn.execute(
            "INSERT INTO outbox(task_id, to_user, text, created_at) VALUES(?,?,?,?)",
            (task_id, to_user, text, int(time.time())))
        self._conn.commit()
        return cur.lastrowid

    def _outbox_row(self, row) -> OutboxItem:
        return OutboxItem(id=row["id"], task_id=row["task_id"], to_user=row["to_user"],
                          text=row["text"], seq=row["seq"], state=row["state"],
                          attempts=row["attempts"], max_attempts=row["max_attempts"],
                          last_error=row["last_error"], created_at=row["created_at"])

    def get_outbox(self, outbox_id: int) -> OutboxItem | None:
        row = self._conn.execute("SELECT * FROM outbox WHERE id=?", (outbox_id,)).fetchone()
        return self._outbox_row(row) if row else None

    def next_outbox_batch(self, limit: int = 10) -> list[OutboxItem]:
        cur = self._conn.execute(
            "UPDATE outbox SET attempts=attempts+1 WHERE id IN "
            "(SELECT id FROM outbox WHERE state='pending' ORDER BY id LIMIT ?) "
            "RETURNING *", (limit,))
        rows = cur.fetchall()
        self._conn.commit()
        return [self._outbox_row(r) for r in rows]

    def mark_sent(self, outbox_id: int) -> None:
        self._conn.execute("UPDATE outbox SET state='sent' WHERE id=?", (outbox_id,))
        self._conn.commit()

    def mark_send_failed(self, outbox_id: int, error: str) -> None:
        row = self._conn.execute(
            "SELECT attempts, max_attempts FROM outbox WHERE id=?", (outbox_id,)).fetchone()
        state = "dead" if row and row["attempts"] >= row["max_attempts"] else "pending"
        self._conn.execute(
            "UPDATE outbox SET state=?, last_error=? WHERE id=?", (state, error, outbox_id))
        self._conn.commit()

    def dead_letter_count(self) -> int:
        return self._conn.execute(
            "SELECT COUNT(*) c FROM outbox WHERE state='dead'").fetchone()["c"]
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_db.py -v`
Expected: 全部 passed（注意 `test_outbox_state_machine` 中凑满 5 次的循环）

- [ ] **Step 5: Commit**

```bash
git add common/ tests/ && git commit -m "feat: tasks/outbox 状态机（claim/重试/死信/崩溃恢复）"
```

---

### Task 5: gateway/ilink.py — iLink 协议客户端

**Files:**
- Create: `gateway/ilink.py`
- Test: `tests/test_ilink.py`

**Interfaces:**
- Produces:
  - `ILinkClient(session: aiohttp.ClientSession, base_url="https://ilinkai.weixin.qq.com")`
  - `await get_bot_qrcode(local_tokens: list[str]) -> dict`
  - `await poll_login_status(qrcode: str, verify_code: str | None = None) -> dict`（返回状态机结果：`{"bot_token","baseurl"}` / `{"already_connected":True}` / `{"expired":True}` / `{"redirect_base":...}` / `{"scanned":True}` / `{"need_verifycode":True}` / `{}` 等待）
  - `await getupdates(buf: str) -> dict`
  - `await sendmessage(to_user, context_token, text) -> bool`（送达确认）
  - `await getconfig(ilink_user_id, context_token) -> str`（typing_ticket）
  - `await sendtyping(ilink_user_id, ticket, status: int) -> None`
  - `make_headers(token=None) -> dict`（模块级，供测试断言）

- [ ] **Step 1: 写失败测试 `tests/test_ilink.py`**

```python
import base64
import json

import pytest
from aioresponses import aioresponses

from gateway.ilink import BASE_URL, ILinkClient

QR_PATH = "/ilink/bot/get_bot_qrcode?bot_type=3"


@pytest.fixture
async def client(aiohttp_client):
    from aiohttp import ClientSession
    async with ClientSession() as s:
        yield ILinkClient(s)


def test_make_headers_shape():
    from gateway.ilink import make_headers
    h = make_headers("TOKEN")
    assert h["Authorization"] == "Bearer TOKEN"
    assert h["AuthorizationType"] == "ilink_bot_token"
    assert h["iLink-App-Id"] == "bot"
    assert h["iLink-App-ClientVersion"] == "132099"
    uin = base64.b64decode(h["X-WECHAT-UIN"]).decode()
    assert uin.isdigit() and 0 <= int(uin) <= 0xFFFFFFFF
    assert make_headers()["X-WECHAT-UIN"] != h["X-WECHAT-UIN"]  # 每次随机


async def test_getupdates_roundtrip(client):
    with aioresponses() as m:
        m.post(f"{BASE_URL}/ilink/bot/getupdates",
               payload={"msgs": [], "get_updates_buf": "BUF1", "errcode": 0},
               headers={"Content-Type": "application/octet-stream"})
        result = await client.getupdates("")
        assert result["get_updates_buf"] == "BUF1"
        req = m.requests[("POST", __import__("yarl").URL(f"{BASE_URL}/ilink/bot/getupdates"))][0]
        body = json.loads(req.kwargs["json"]) if "json" in req.kwargs else json.loads(req.kwargs["data"])
        assert body["base_info"]["channel_version"] == "2.4.3"
        assert "get_updates_buf" in body


async def test_sendmessage_full_fields(client):
    with aioresponses() as m:
        m.post(f"{BASE_URL}/ilink/bot/sendmessage", payload={})
        ok = await client.sendmessage("u@im.wechat", "CTX", "你好")
        assert ok is True
        req = list(m.requests.values())[0][0]
        body = json.loads(req.kwargs["json"]) if "json" in req.kwargs else json.loads(req.kwargs["data"])
        msg = body["msg"]
        assert msg["from_user_id"] == ""           # 全字段断言（静默不投递防线）
        assert msg["to_user_id"] == "u@im.wechat"
        assert msg["client_id"].startswith("daoyu-") and len(msg["client_id"]) == 15
        assert msg["message_type"] == 2 and msg["message_state"] == 2
        assert msg["context_token"] == "CTX"
        assert msg["item_list"] == [{"type": 1, "text_item": {"text": "你好"}}]
        assert body["base_info"]["bot_agent"].startswith("daoyu/")


async def test_sendmessage_errcode_is_failure(client):
    with aioresponses() as m:
        m.post(f"{BASE_URL}/ilink/bot/sendmessage", payload={"errcode": 500, "errmsg": "boom"})
        assert await client.sendmessage("u", "c", "t") is False


async def test_sendmessage_http_error_is_failure(client):
    with aioresponses() as m:
        m.post(f"{BASE_URL}/ilink/bot/sendmessage", status=502, payload={})
        assert await client.sendmessage("u", "c", "t") is False


async def test_getconfig_typing_ticket(client):
    with aioresponses() as m:
        m.post(f"{BASE_URL}/ilink/bot/getconfig", payload={"typing_ticket": "TK"})
        assert await client.getconfig("u", "CTX") == "TK"


async def test_login_status_states(client):
    cases = [
        ({"status": "confirmed", "bot_token": "T", "baseurl": "https://x"},
         {"bot_token": "T", "baseurl": "https://x"}),
        ({"status": "expired"}, {"expired": True}),
        ({"status": "binded_redirect"}, {"already_connected": True}),
        ({"status": "scaned_but_redirect", "redirect_host": "h.example"},
         {"redirect_base": "https://h.example"}),
        ({"status": "scanned"}, {"scanned": True}),
        ({"status": "need_verifycode"}, {"need_verifycode": True}),
        ({"status": "wait"}, {}),
    ]
    for payload, expected in cases:
        with aioresponses() as m:
            m.get(f"{BASE_URL}/ilink/bot/get_qrcode_status?qrcode=Q", payload=payload)
            assert await client.poll_login_status("Q") == expected


async def test_get_bot_qrcode_post_first(client):
    with aioresponses() as m:
        m.post(f"{BASE_URL}{QR_PATH}", payload={"qrcode": "Q", "qrcode_img_content": "https://img"})
        data = await client.get_bot_qrcode([])
        assert data["qrcode"] == "Q"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_ilink.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现 `gateway/ilink.py`**

```python
"""iLink (ClawBot) 协议封装。纯协议、无业务；字段必须全填（缺字段会 200 但静默不投递）。"""
import base64
import random

import aiohttp

BASE_URL = "https://ilinkai.weixin.qq.com"
ILINK_APP_ID = "bot"
ILINK_APP_CLIENT_VERSION = str((2 << 16) | (4 << 8) | 3)  # "2.4.3" → "132099"
CHANNEL_VERSION = "2.4.3"
BOT_AGENT = "daoyu/0.1.0 (python)"


def make_headers(token: str | None = None) -> dict:
    uin = str(random.randint(0, 0xFFFFFFFF))
    headers = {
        "Content-Type": "application/json",
        "AuthorizationType": "ilink_bot_token",
        "X-WECHAT-UIN": base64.b64encode(uin.encode()).decode(),
        "iLink-App-Id": ILINK_APP_ID,
        "iLink-App-ClientVersion": ILINK_APP_CLIENT_VERSION,
    }
    if token:
        headers["Authorization"] = f"Bearer {token}"
    return headers


def base_info() -> dict:
    return {"channel_version": CHANNEL_VERSION, "bot_agent": BOT_AGENT}


class ILinkError(Exception):
    pass


class ILinkClient:
    def __init__(self, session: aiohttp.ClientSession, base_url: str = BASE_URL):
        self._session = session
        self._base = base_url.rstrip("/")

    async def _post(self, path: str, body: dict, token: str | None = None,
                    base_url: str | None = None) -> dict:
        url = f"{base_url or self._base}/{path}"
        async with self._session.post(url, json=body, headers=make_headers(token)) as res:
            # 服务器 Content-Type 是 application/octet-stream，必须 content_type=None
            data = await res.json(content_type=None)
            if res.status != 200:
                raise ILinkError(f"POST {path} HTTP {res.status}: {data}")
            return data

    async def _get(self, path: str, token: str | None = None) -> dict:
        url = f"{self._base}/{path}"
        async with self._session.get(url, headers=make_headers(token)) as res:
            data = await res.json(content_type=None)
            if res.status != 200:
                raise ILinkError(f"GET {path} HTTP {res.status}: {data}")
            return data

    # ---- 登录 ----
    async def get_bot_qrcode(self, local_tokens: list[str], base_url: str | None = None) -> dict:
        data = await self._post("ilink/bot/get_bot_qrcode?bot_type=3",
                                {"local_token_list": local_tokens}, None, base_url)
        if not data.get("qrcode"):
            data = await self._get("ilink/bot/get_bot_qrcode?bot_type=3")  # 旧版 GET 兜底
        if not data.get("qrcode"):
            raise ILinkError(f"get_bot_qrcode 无 qrcode: {data}")
        return data

    async def poll_login_status(self, qrcode: str, verify_code: str | None = None) -> dict:
        from urllib.parse import quote
        endpoint = f"ilink/bot/get_qrcode_status?qrcode={quote(qrcode, safe='')}"
        if verify_code:
            endpoint += f"&verify_code={quote(verify_code, safe='')}"
        status = await self._get(endpoint)
        state = status.get("status", "")
        if state == "confirmed" or status.get("bot_token"):
            return {"bot_token": status.get("bot_token"),
                    "baseurl": status.get("baseurl") or status.get("base_url") or self._base}
        if state == "binded_redirect" or status.get("binded_redirect"):
            return {"already_connected": True}
        if state == "expired":
            return {"expired": True}
        if state == "scaned_but_redirect":
            host = status.get("redirect_host")
            return {"redirect_base": f"https://{host}"} if host else {}
        if state == "scanned":
            return {"scanned": True}
        if state == "need_verifycode":
            return {"need_verifycode": True}
        if state == "verify_code_blocked":
            return {"verify_code_blocked": True}
        return {}

    # ---- 收发 ----
    async def getupdates(self, buf: str, token: str, base_url: str | None = None) -> dict:
        return await self._post(
            "ilink/bot/getupdates",
            {"get_updates_buf": buf, "base_info": base_info()}, token, base_url)

    async def sendmessage(self, to_user: str, context_token: str, text: str,
                          token: str, base_url: str | None = None) -> bool:
        client_id = f"daoyu-{random.randint(0, 0xFFFFFFFF):08x}"
        data = await self._post(
            "ilink/bot/sendmessage",
            {"msg": {
                "from_user_id": "",
                "to_user_id": to_user,
                "client_id": client_id,
                "message_type": 2,
                "message_state": 2,
                "context_token": context_token,
                "item_list": [{"type": 1, "text_item": {"text": text}}],
            },
                "base_info": base_info()}, token, base_url)
        errcode = data.get("errcode", 0)
        if errcode:
            raise_or_note = f"errcode={errcode} errmsg={data.get('errmsg')}"
            import logging
            logging.getLogger(__name__).warning("sendmessage 被拒: %s", raise_or_note)
        return not errcode

    async def getconfig(self, ilink_user_id: str, context_token: str,
                        token: str, base_url: str | None = None) -> str:
        data = await self._post(
            "ilink/bot/getconfig",
            {"ilink_user_id": ilink_user_id, "context_token": context_token,
             "base_info": base_info()}, token, base_url)
        return data.get("typing_ticket", "")

    async def sendtyping(self, ilink_user_id: str, ticket: str, status: int,
                         token: str, base_url: str | None = None) -> None:
        await self._post(
            "ilink/bot/sendtyping",
            {"ilink_user_id": ilink_user_id, "typing_ticket": ticket,
             "status": status, "base_info": base_info()}, token, base_url)
```

（注：token/base_url 作为参数传入而非存实例状态，调用方持有 `bot_token_ref` 语义；这样测试与服务端重连替换都简单。）

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_ilink.py -v`
Expected: 全部 passed

- [ ] **Step 5: Commit**

```bash
git add gateway/ tests/ && git commit -m "feat: iLink 协议客户端（全字段 sendmessage/登录状态机/typing）"
```

---

### Task 6: gateway/router.py — 统一命令路由

**Files:**
- Create: `gateway/router.py`
- Test: `tests/test_router.py`

**Interfaces:**
- Consumes: 无（纯函数）。
- Produces:
  - `Route` dataclass：`kind`（"bridge"|"ilink"|"forward"|"proxy"|"unknown"|"chat"）、`command: str | None`、`args: str`、`detail: dict`
  - `route(text: str, slash_commands: set[str]) -> Route`
  - 常量 `BRIDGE_COMMANDS = {"cancel","tasks","status","cd","policy"}`、`ILINK_COMMANDS = {"time","重新连接","help"}`、`PROXY_COMMANDS = {"permissions","hooks","plugins","login","config","mcp","vim","terminal-setup"}`

- [ ] **Step 1: 写失败测试 `tests/test_router.py`**

```python
from gateway.router import route


def test_plain_text_is_chat():
    r = route("帮我看下这个 bug", set())
    assert r.kind == "chat"


def test_bridge_commands():
    for cmd, args in [("cancel", ""), ("tasks", ""), ("/status", ""), ("cd", "/repo"), ("policy", "strict")]:
        text = cmd if cmd.startswith("/") else f"/{cmd} {args}".strip()
        r = route(text, set())
        assert r.kind == "bridge", text
    r = route("/policy strict", set())
    assert r.command == "policy" and r.args == "strict"


def test_ilink_ops():
    assert route("/time", set()).kind == "ilink"
    assert route("/重新连接", set()).kind == "ilink"
    assert route("/help", set()).kind == "ilink"


def test_known_slash_command_forwards():
    r = route("/review", {"review", "model", "compact"})
    assert r.kind == "forward" and r.command == "review"


def test_proxy_tui_commands():
    for c in ("permissions", "hooks", "plugins", "login", "config", "mcp"):
        r = route(f"/{c}", set())
        assert r.kind == "proxy", c


def test_forward_beats_proxy_when_in_slash_commands():
    # /mcp 若 headless 实际可用（出现在 slash_commands），优先转发
    r = route("/mcp", {"mcp"})
    assert r.kind == "forward"


def test_unknown_gets_suggestion():
    r = route("/revie w", {"review"})
    assert r.kind == "unknown"
    assert "review" in r.args


def test_forward_with_args_kept_verbatim():
    r = route("/model sonnet", {"model"})
    assert r.kind == "forward" and r.args == "sonnet"


def test_bare_slash_treated_as_unknown():
    r = route("/", set())
    assert r.kind == "unknown"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_router.py -v`
Expected: FAIL

- [ ] **Step 3: 实现 `gateway/router.py`**

```python
"""统一命令总线路由（TRD §5）。顺序：桥命令 → iLink 运维 → 转发（headless 可用集）→ 代理 → 未知。"""
import difflib
from dataclasses import dataclass

BRIDGE_COMMANDS = {"cancel", "tasks", "status", "cd", "policy"}
ILINK_COMMANDS = {"time", "重新连接", "help"}
# TUI 交互专属（静态维护，官方 commands 文档），M2 提供完整代理实现
PROXY_COMMANDS = {"permissions", "hooks", "plugins", "login", "config", "mcp",
                  "vim", "terminal-setup"}


@dataclass
class Route:
    kind: str            # chat / bridge / ilink / forward / proxy / unknown
    command: str | None
    args: str
    detail: dict


def _closest(name: str, candidates: set[str]) -> str | None:
    matches = difflib.get_close_matches(name, candidates, n=1, cutoff=0.6)
    return matches[0] if matches else None


def route(text: str, slash_commands: set[str]) -> Route:
    stripped = text.strip()
    if not stripped.startswith("/"):
        return Route(kind="chat", command=None, args=stripped, detail={})

    body = stripped[1:]
    parts = body.split(None, 1)
    name = parts[0] if parts else ""
    args = parts[1] if len(parts) > 1 else ""
    if not name:
        return Route(kind="unknown", command=None, args="命令为空",
                     detail={"suggestion": None})

    if name in BRIDGE_COMMANDS:
        return Route(kind="bridge", command=name, args=args, detail={})
    if name in ILINK_COMMANDS:
        return Route(kind="ilink", command=name, args=args, detail={})
    if name in slash_commands:
        return Route(kind="forward", command=name, args=args, detail={})
    if name in PROXY_COMMANDS:
        return Route(kind="proxy", command=name, args=args, detail={})

    pool = BRIDGE_COMMANDS | ILINK_COMMANDS | PROXY_COMMANDS | set(slash_commands)
    suggestion = _closest(name, pool)
    return Route(kind="unknown", command=name, args=args,
                 detail={"suggestion": suggestion})
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_router.py -v`
Expected: 全部 passed

- [ ] **Step 5: Commit**

```bash
git add gateway/ tests/ && git commit -m "feat: 统一命令路由（桥/iLink/转发/代理/未知+建议）"
```

---

### Task 7: worker/cli_builder.py — claude argv 组装

**Files:**
- Create: `worker/cli_builder.py`
- Test: `tests/test_cli_builder.py`

**Interfaces:**
- Consumes: `common/models.Budget`。
- Produces: `build_argv(*, session_uuid, resume, policy, budget, mcp_config: Path | None, settings: Path | None) -> list[str]`（不含可执行前缀，只有 flag 部分；prompt 走 stdin 不进 argv）

- [ ] **Step 1: 写失败测试 `tests/test_cli_builder.py`**

```python
from pathlib import Path

from common.models import Budget
from worker.cli_builder import build_argv


def test_new_session_uses_session_id():
    argv = build_argv(session_uuid="U", resume=False, policy="auto",
                      budget=Budget(), mcp_config=None, settings=None)
    assert argv[0] == "-p"
    assert "--session-id" in argv and argv[argv.index("--session-id") + 1] == "U"
    assert "--resume" not in argv


def test_resume_session():
    argv = build_argv(session_uuid="U", resume=True, policy="auto",
                      budget=Budget(), mcp_config=None, settings=None)
    assert "--resume" in argv and argv[argv.index("--resume") + 1] == "U"
    assert "--session-id" not in argv


def test_policy_mode_mapping():
    def mode_for(policy):
        argv = build_argv(session_uuid="U", resume=True, policy=policy,
                          budget=Budget(), mcp_config=None, settings=None)
        return argv[argv.index("--permission-mode") + 1]
    assert mode_for("auto") == "acceptEdits"
    assert mode_for("strict") == "acceptEdits"
    assert mode_for("bypass") == "bypassPermissions"
    assert mode_for("plan") == "plan"


def test_bypass_adds_disallowed_tools_fallback():
    argv = build_argv(session_uuid="U", resume=True, policy="bypass",
                      budget=Budget(), mcp_config=None, settings=None)
    assert "--disallowedTools" in argv   # deny 清单在 bypass 下是否生效未实测，工具级兜底恒加
    argv2 = build_argv(session_uuid="U", resume=True, policy="auto",
                       budget=Budget(), mcp_config=None, settings=None)
    assert "--disallowedTools" not in argv2


def test_budget_and_stream_flags_always_present():
    for policy in ("auto", "strict", "bypass", "plan"):
        argv = build_argv(session_uuid="U", resume=True, policy=policy,
                          budget=Budget(max_turns=30, max_usd=1.5),
                          mcp_config=Path("/mcp.json"), settings=Path("/settings.json"))
        assert argv[argv.index("--max-turns") + 1] == "30"
        assert argv[argv.index("--max-budget-usd") + 1] == "1.5"
        assert "--bare" in argv
        assert argv[argv.index("--output-format") + 1] == "stream-json"
        assert "--verbose" in argv
        assert "--include-partial-messages" in argv
        assert argv[argv.index("--mcp-config") + 1] == str(Path("/mcp.json"))
        assert argv[argv.index("--settings") + 1] == str(Path("/settings.json"))
        assert "--strict-mcp-config" in argv
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_cli_builder.py -v`
Expected: FAIL

- [ ] **Step 3: 实现 `worker/cli_builder.py`**

```python
"""claude CLI 命令行组装（TRD §4.1）。每次调用全量传 flag（--resume 不恢复权限/MCP 配置）。
prompt 一律走 stdin，不进 argv（避免 shell 转义问题）。"""
from pathlib import Path

from common.models import Budget

POLICY_MODE = {
    "auto": "acceptEdits",
    "strict": "acceptEdits",        # 审批 MCP 为 M2 项；M1 strict 与 auto 同基线
    "bypass": "bypassPermissions",
    "plan": "plan",
}

# bypass 档工具级兜底（bypass 下 permissions.deny 生效性未实测，TRD §8 要求叠加）
BYPASS_DISALLOWED_TOOLS = [
    "Read(/etc/**)", "Read(~/.ssh/**)", "Read(~/.claude/**)",
    "Edit(/etc/**)", "Edit(~/.ssh/**)", "Edit(~/.claude/**)", "Edit(./data/**)",
]


def build_argv(*, session_uuid: str, resume: bool, policy: str, budget: Budget,
               mcp_config: Path | None, settings: Path | None) -> list[str]:
    argv = ["-p"]
    argv += ["--resume", session_uuid] if resume else ["--session-id", session_uuid]
    argv += ["--permission-mode", POLICY_MODE[policy]]
    if policy == "bypass":
        argv += ["--disallowedTools", ",".join(BYPASS_DISALLOWED_TOOLS)]
    argv += ["--bare"]
    argv += ["--max-turns", str(budget.max_turns)]
    argv += ["--max-budget-usd", str(budget.max_usd)]
    if mcp_config is not None:
        argv += ["--mcp-config", str(mcp_config), "--strict-mcp-config"]
    if settings is not None:
        argv += ["--settings", str(settings)]
    argv += ["--output-format", "stream-json", "--verbose",
             "--include-partial-messages"]
    return argv
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_cli_builder.py -v`
Expected: 全部 passed

- [ ] **Step 5: Commit**

```bash
git add worker/ tests/ && git commit -m "feat: policy/预算 → claude argv 组装（全量 flag、bypass 工具级兜底）"
```

---

### Task 8: worker/stream.py — stream-json 解析与节流

**Files:**
- Create: `worker/stream.py`、`tests/fixtures/review_stream.jsonl`
- Test: `tests/test_stream.py`

**Interfaces:**
- Produces:
  - `StreamEvent` dataclass：`type`（"init"/"text"/"tool"/"result"）、`text: str`、`tool_name: str | None`、`slash_commands: list[str]`、`session_id: str | None`、`cost_usd: float | None`、`is_error: bool`
  - `StreamParser.feed_line(line: str) -> StreamEvent | None`
  - `Throttle(interval_s: float)`：`.allow(now: float | None = None) -> bool`

- [ ] **Step 1: 写 fixture `tests/fixtures/review_stream.jsonl`（模拟 review 任务的流）**

```jsonl
{"type":"system","subtype":"init","session_id":"abc-123","slash_commands":["review","model","compact","help","usage"]}
{"type":"stream_event","event":{"type":"content_block_start","index":0,"content_block":{"type":"text"}}}
{"type":"stream_event","event":{"type":"content_block_delta","index":0,"delta":{"type":"text_delta","text":"我来审查这个仓库。"}}}
{"type":"stream_event","event":{"type":"content_block_start","index":1,"content_block":{"type":"tool_use","id":"t1","name":"Bash"}}}
{"type":"stream_event","event":{"type":"content_block_delta","index":1,"delta":{"type":"input_json_delta","partial_json":"{\"command\":\"git log --oneline\"}"}}}
{"type":"stream_event","event":{"type":"content_block_delta","index":0,"delta":{"type":"text_delta","text":"继续分析。"}}}
{"type":"stream_event","event":{"type":"content_block_start","index":2,"content_block":{"type":"tool_use","id":"t2","name":"Read"}}}
{"type":"result","subtype":"success","result":"审查完成：3 个问题。","total_cost_usd":0.21,"is_error":false}
```

- [ ] **Step 2: 写失败测试 `tests/test_stream.py`**

```python
from pathlib import Path

from worker.stream import StreamParser, Throttle

FIXTURE = Path(__file__).parent / "fixtures" / "review_stream.jsonl"


def parse_all():
    p = StreamParser()
    events = []
    for line in FIXTURE.read_text(encoding="utf-8").splitlines():
        ev = p.feed_line(line)
        if ev:
            events.append(ev)
    return events


def test_init_event_extracts_slash_commands_and_session():
    init = next(e for e in parse_all() if e.type == "init")
    assert init.session_id == "abc-123"
    assert "review" in init.slash_commands and "model" in init.slash_commands


def test_text_and_tool_events():
    events = parse_all()
    texts = [e.text for e in events if e.type == "text"]
    assert texts == ["我来审查这个仓库。", "继续分析。"]
    tools = [(e.tool_name, e.text) for e in events if e.type == "tool"]
    assert tools[0][0] == "Bash"
    assert "git log" in tools[0][1]
    assert tools[1][0] == "Read"


def test_result_event():
    r = parse_all()[-1]
    assert r.type == "result" and r.text == "审查完成：3 个问题。"
    assert r.cost_usd == 0.21 and r.is_error is False


def test_malformed_line_ignored():
    p = StreamParser()
    assert p.feed_line("not json") is None
    assert p.feed_line("") is None
    assert p.feed_line('{"type":"stream_event","event":{"type":"ping"}}') is None


def test_throttle_window():
    t = Throttle(interval_s=2.5)
    assert t.allow(now=0.0) is True      # 第一条立即放行
    assert t.allow(now=1.0) is False     # 窗口内抑制
    assert t.allow(now=2.6) is True      # 出窗口放行
    assert t.allow(now=3.0) is False
```

- [ ] **Step 3: 跑测试确认失败**

Run: `python -m pytest tests/test_stream.py -v`
Expected: FAIL

- [ ] **Step 4: 实现 `worker/stream.py`**

```python
"""claude -p --output-format stream-json 的 NDJSON 事件解析 + 进度节流。"""
import json
import time
from dataclasses import dataclass, field


@dataclass
class StreamEvent:
    type: str                                  # init / text / tool / result
    text: str = ""
    tool_name: str | None = None
    slash_commands: list[str] = field(default_factory=list)
    session_id: str | None = None
    cost_usd: float | None = None
    is_error: bool = False


class StreamParser:
    """feed_line 喂入 stdout 的一行（含/不含换行均可），产出关注的事件，忽略其余。"""

    def feed_line(self, line: str) -> StreamEvent | None:
        line = line.strip()
        if not line:
            return None
        try:
            obj = json.loads(line)
        except ValueError:
            return None

        t = obj.get("type")
        if t == "system" and obj.get("subtype") == "init":
            return StreamEvent(type="init", session_id=obj.get("session_id"),
                               slash_commands=list(obj.get("slash_commands") or []))
        if t == "stream_event":
            ev = obj.get("event") or {}
            et = ev.get("type")
            if et == "content_block_delta":
                delta = ev.get("delta") or {}
                if delta.get("type") == "text_delta":
                    return StreamEvent(type="text", text=delta.get("text", ""))
            elif et == "content_block_start":
                block = ev.get("content_block") or {}
                if block.get("type") == "tool_use":
                    return StreamEvent(type="tool", tool_name=block.get("name"))
            return None
        if t == "result":
            return StreamEvent(type="result", text=obj.get("result") or "",
                               cost_usd=obj.get("total_cost_usd"),
                               is_error=bool(obj.get("is_error")))
        return None


class Throttle:
    """时间窗节流：窗口内合并、出窗口放行第一条。"""

    def __init__(self, interval_s: float = 2.5):
        self._interval = interval_s
        self._last = 0.0

    def allow(self, now: float | None = None) -> bool:
        now = time.monotonic() if now is None else now
        if now - self._last >= self._interval:
            self._last = now
            return True
        return False
```

- [ ] **Step 5: 跑测试确认通过**

Run: `python -m pytest tests/test_stream.py -v`
Expected: 全部 passed

- [ ] **Step 6: Commit**

```bash
git add worker/ tests/ && git commit -m "feat: stream-json 事件解析（init/text/tool/result）+ 节流器"
```

---

### Task 9: worker/runner.py — 子进程执行

**Files:**
- Create: `worker/runner.py`、`tests/fixtures/fake_claude.py`
- Test: `tests/test_runner.py`

**Interfaces:**
- Consumes: Task 7 的 `build_argv`、Task 8 的 `StreamParser`/`Throttle`、Task 2-4 的 Database。
- Produces:
  - `TaskRunner(db, config, process_registry: dict[int, asyncio.subprocess.Process])`
  - `await runner.run(task: Task, session: SessionBinding) -> None`（完成后任务状态已写、最终回复已入 outbox）
  - config 需含：`claude_bin: str | list[str]`、`secrets: dict`（含 ANTHROPIC_API_KEY）、`repo_root: Path`（定位 claude/mcp.json 等）、`throttle.progress_window_s`、`budget`（Budget）
  - 进度推送格式：工具事件节流合并为 `"🔧 Bash $ git log --oneline"` 多行文本入 outbox（task_id 关联）

- [ ] **Step 1: 写 fake claude `tests/fixtures/fake_claude.py`**

```python
"""假 claude CLI：读 stdin 收 prompt，逐行回放 FAKE_CLAUDE_SCRIPT 指向的 NDJSON 文件。"""
import os
import sys
import time

def main():
    prompt = sys.stdin.read()          # 必须消费 stdin（真实 claude 也从 stdin 读）
    script = os.environ["FAKE_CLAUDE_SCRIPT"]
    # 把收到的 prompt 存下来供测试断言
    with open(os.environ["FAKE_CLAUDE_STDIN_LOG"], "w", encoding="utf-8") as f:
        f.write(prompt)
    if os.environ.get("FAKE_CLAUDE_STDERR"):
        sys.stderr.write(os.environ["FAKE_CLAUDE_STDERR"])
    with open(script, encoding="utf-8") as f:
        for line in f:
            sys.stdout.write(line)
            sys.stdout.flush()
            time.sleep(float(os.environ.get("FAKE_CLAUDE_DELAY", "0.01")))

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 写失败测试 `tests/test_runner.py`**

```python
import asyncio
import json
import os
import sys
from pathlib import Path

import pytest

from common.models import Budget
from worker.runner import TaskRunner

FIXTURES = Path(__file__).parent / "fixtures"


class FakeConfig:
    def __init__(self, tmp_path, monkeypatch):
        self.claude_bin = [sys.executable, str(FIXTURES / "fake_claude.py")]
        self.secrets = {"ANTHROPIC_API_KEY": "sk-test"}
        self.repo_root = tmp_path
        self.throttle = {"progress_window_s": 0.0}   # 测试窗口=0 → 每条都推
        self.budget = Budget(max_turns=10, max_usd=1.0)
        self.page_char_limit = 2000
        self.fake_script = str(FIXTURES / "review_stream.jsonl")
        self.stdin_log = tmp_path / "stdin.log"
        monkeypatch.setenv("FAKE_CLAUDE_SCRIPT", self.fake_script)
        monkeypatch.setenv("FAKE_CLAUDE_STDIN_LOG", str(self.stdin_log))


@pytest.fixture
def cfg(tmp_path, monkeypatch):
    return FakeConfig(tmp_path, monkeypatch)


async def test_run_task_success(db, cfg):
    s = db.get_or_create_session("u@im.wechat", str(cfg.repo_root))
    t = db.create_task(None, s.id, "/review", kind="command")
    runner = TaskRunner(db, cfg, process_registry={})
    await runner.run(db.get_task(t), s)

    assert db.get_task(t).state == "done"
    # stdin 收到原样 prompt（不经过 shell）
    assert cfg.stdin_log.read_text(encoding="utf-8") == "/review"
    # 最终回复入 outbox（含 result 文本）
    sent = [db.get_outbox(i) for i in _outbox_ids(db)]
    final = [o for o in sent if "审查完成" in o.text]
    assert final, f"未找到最终回复: {[o.text for o in sent]}"
    # 费用入审计
    assert db.today_cost_usd() == pytest.approx(0.21)
    # slash_commands 已同步进 state
    assert "review" in (db.get_state("slash_commands") or "")
    # 进度（工具事件）也推过
    progress = [o for o in sent if "Bash" in o.text]
    assert progress


def _outbox_ids(db):
    rows = db._conn.execute("SELECT id FROM outbox ORDER BY id").fetchall()
    return [r["id"] for r in rows]


async def test_run_task_subprocess_fail(db, cfg, monkeypatch):
    monkeypatch.setenv("FAKE_CLAUDE_STDERR", "boom")
    cfg.claude_bin = [sys.executable, "-c",
                      "import sys; sys.stdin.read(); sys.exit(1)"]
    s = db.get_or_create_session("u@im.wechat", str(cfg.repo_root))
    t = db.create_task(None, s.id, "hello")
    runner = TaskRunner(db, cfg, process_registry={})
    await runner.run(db.get_task(t), s)
    # 子进程失败 → 任务 failed（回 pending 或 dead 由 attempts 决定）+ 错误回复入 outbox
    assert db.get_task(t).state in ("failed", "pending")
    assert any("失败" in (db.get_outbox(i).text or "")
               for i in _outbox_ids(db))


async def test_cancel_kills_process(db, cfg, monkeypatch):
    # 长运行 fake（slow 模式）：echo 一行后 hang
    hang = FIXTURES / "hang_claude.py"
    hang.write_text("import sys, time\nsys.stdin.read()\n"
                    "print('{\"type\":\"result\",\"result\":\"never\"}', flush=True)\n"
                    "time.sleep(300)\n", encoding="utf-8")
    monkeypatch.setenv("FAKE_CLAUDE_SCRIPT", str(FIXTURES / "review_stream.jsonl"))
    cfg.claude_bin = [sys.executable, str(hang)]
    cfg.throttle = {"progress_window_s": 0.5}
    s = db.get_or_create_session("u@im.wechat", str(cfg.repo_root))
    t = db.create_task(None, s.id, "long")
    registry = {}
    runner = TaskRunner(db, cfg, process_registry=registry)
    task_async = asyncio.create_task(runner.run(db.get_task(t), s))
    await asyncio.sleep(0.5)          # 等子进程注册
    assert t in registry
    registry[t].kill()
    await asyncio.wait_for(task_async, timeout=5)
    assert db.get_task(t).state in ("canceled", "failed")
```

- [ ] **Step 3: 跑测试确认失败**

Run: `python -m pytest tests/test_runner.py -v`
Expected: FAIL

- [ ] **Step 4: 实现 `worker/runner.py`**

```python
"""单任务执行：组装 argv → 子进程 → 解析流 → 节流推进度 → 最终回复入 outbox。"""
import asyncio
import os

from worker.cli_builder import build_argv
from worker.stream import StreamParser, Throttle


def split_text(text: str, limit: int) -> list[str]:
    """超长文本分页（M1 按 UTF-16 代理对安全切分——按字符数切）。"""
    if len(text) <= limit:
        return [text]
    pages = [text[i:i + limit] for i in range(0, len(text), limit)]
    return [f"(第 {i}/{len(pages)} 页)\n{p}" for i, p in enumerate(pages, 1)]


class TaskRunner:
    def __init__(self, db, config, process_registry: dict):
        self._db = db
        self._cfg = config
        self._procs = process_registry

    async def run(self, task, session) -> None:
        # 该 Claude 会话是否已被首次调用过（--session-id 建立后才能 --resume；
        # 对不存在的 UUID 直接 --resume 会报错，所以必须显式记录）
        resume = self._db.get_state(f"claude_session_inited:{session.claude_uuid}") is not None
        argv = build_argv(
            session_uuid=session.claude_uuid,
            resume=resume,
            policy=session.policy,
            budget=self._cfg.budget,
            mcp_config=self._cfg.repo_root / "claude" / "mcp.json",
            settings=self._cfg.repo_root / "claude" / "settings.json",
        )
        bin_ = self._cfg.claude_bin
        prefix = bin_ if isinstance(bin_, list) else [bin_]
        env = os.environ.copy()
        env.update(self._cfg.secrets)

        try:
            proc = await asyncio.create_subprocess_exec(
                *prefix, *argv,
                stdin=asyncio.subprocess.PIPE,
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
                cwd=session.cwd, env=env)
        except OSError as e:
            await self._fail(task, f"无法启动 claude 子进程: {e}")
            return

        self._procs[task.id] = proc
        try:
            stdout, _stderr = await proc.communicate(task.prompt.encode("utf-8"))
        finally:
            self._procs.pop(task.id, None)

        if proc.returncode != 0 and self._db.get_task(task.id).state == "canceled":
            self._push(task, session.wechat_user, "已取消。")
            return

        await self._consume(task, session, stdout.decode("utf-8", "replace"))

        if proc.returncode != 0:
            await self._fail(task, session.wechat_user, f"claude 退出码 {proc.returncode}")
            return
        self._db.finish_task(task.id, "done")
        self._db.set_state(f"claude_session_inited:{session.claude_uuid}", "1")
        self._db.touch_session(session.id)

    async def _consume(self, task, session, raw: str) -> None:
        parser = StreamParser()
        throttle = Throttle(self._cfg.throttle["progress_window_s"])
        pending_lines: list[str] = []
        result_text = ""
        cost = None

        for line in raw.splitlines():
            ev = parser.feed_line(line)
            if ev is None:
                continue
            if ev.type == "init":
                if ev.slash_commands:
                    import json
                    self._db.set_state("slash_commands", json.dumps(ev.slash_commands))
                    if ev.session_id and ev.session_id != session.claude_uuid:
                        self._db.audit("session_uuid_drift",
                                       f"expected={session.claude_uuid} got={ev.session_id}")
            elif ev.type == "tool":
                pending_lines.append(f"🔧 {ev.tool_name}")
            elif ev.type == "text":
                pass  # 增量文本不推（微信端不可编辑，等最终版）；如需可在此节流推送
            elif ev.type == "result":
                result_text = ev.text
                cost = ev.cost_usd
                if pending_lines and throttle.allow():
                    self._push(task, "\n".join(pending_lines))
                    pending_lines.clear()

            if pending_lines and throttle.allow():
                self._push(task, "\n".join(pending_lines))
                pending_lines.clear()

        if cost is not None:
            import json
            self._db.audit("cost", json.dumps({"task_id": task.id, "usd": cost}))
        for page in split_text(result_text or "(空回复)", self._cfg.page_char_limit):
            self._push(task, session.wechat_user, page)

    def _push(self, task, to_user: str, text: str) -> None:
        self._db.enqueue(task.id, to_user, text)

    async def _fail(self, task, to_user: str, err: str) -> None:
        self._db.finish_task(task.id, "failed")
        self._db.audit("task_failed", f"task={task.id} err={err}")
        self._push(task, to_user, f"❌ 任务失败：{err}")
```

**注意**：`_consume` 内部的 `self._push(task, ...)` 调用同样要带上 `session.wechat_user`（`_consume` 已有 session 参数）。

**注意（重要）**：上面 runner 代码中 `resume` 判定使用 state KV `claude_session_inited:<uuid>`（任务成功后置位），**不能**用 `task.attempts > 0` 判定——`claim_next_pending` 在领取时已把 attempts 置为 ≥1，新任务也会命中。

- [ ] **Step 5: 修正后跑测试确认通过**

Run: `python -m pytest tests/test_runner.py -v`
Expected: 全部 passed

- [ ] **Step 6: Commit**

```bash
git add worker/ tests/ && git commit -m "feat: TaskRunner 子进程执行（stdin prompt/流解析/节流进度/费用记账）"
```

---

### Task 10: worker/pool.py — 调度池

**Files:**
- Create: `worker/pool.py`
- Test: `tests/test_pool.py`

**Interfaces:**
- Consumes: TaskRunner。
- Produces:
  - `WorkerPool(db, config, runner: TaskRunner | None = None, concurrency=3, poll_interval_s=0.5)`
  - `await run_forever()`（可被 cancel）
  - `await submit_check() -> None`（有新任务时唤醒一次扫描，测试用）
  - `running_session_ids() -> set[int]`
  - `async cancel(task_id: int) -> str`（返回给用户的回复文本；pending 直接置 canceled，running kill 子进程）
  - `snapshot() -> list[Task]`（运行中+排队，供 /tasks）

- [ ] **Step 1: 写失败测试 `tests/test_pool.py`**

```python
import asyncio

import pytest

from worker.pool import WorkerPool


class FakeRunner:
    def __init__(self):
        self.started = asyncio.Event()
        self.finish = asyncio.Event()
        self.ran = []
        self.procs = {}

    async def run(self, task, session):
        self.ran.append(task.id)
        self.started.set()
        await self.finish.wait()


@pytest.fixture
def fake_runner():
    return FakeRunner()


def make_pool(db, runner, **kw):
    return WorkerPool(db, config=None, runner=runner, concurrency=2,
                      poll_interval_s=0.01, **kw)


async def test_same_session_serial(db, fake_runner):
    s = db.get_or_create_session("u@im.wechat", "/repo")
    db.create_task(None, s.id, "a")
    db.create_task(None, s.id, "b")
    pool = make_pool(db, fake_runner)
    loop_task = asyncio.create_task(pool.run_forever())
    await asyncio.wait_for(fake_runner.started.wait(), 3)
    await asyncio.sleep(0.2)
    assert fake_runner.ran == [1]                     # 同 session 第二个不启动
    assert pool.running_session_ids() == {s.id}
    fake_runner.finish.set()
    for _ in range(100):                      # 等第二个任务被领取
        if fake_runner.ran == [1, 2]:
            break
        await asyncio.sleep(0.05)
    assert fake_runner.ran == [1, 2]
    loop_task.cancel()


async def test_different_sessions_parallel(db, fake_runner):
    s1 = db.get_or_create_session("u@im.wechat", "/a")
    s2 = db.get_or_create_session("u@im.wechat", "/b")
    db.create_task(None, s1.id, "a")
    db.create_task(None, s2.id, "b")
    pool = make_pool(db, fake_runner)
    loop_task = asyncio.create_task(pool.run_forever())
    await asyncio.wait_for(fake_runner.started.wait(), 3)
    await asyncio.sleep(0.2)
    assert sorted(fake_runner.ran) == [1, 2]          # 跨 session 并行
    fake_runner.finish.set()
    loop_task.cancel()


async def test_concurrency_cap(db, fake_runner):
    sessions = [db.get_or_create_session("u@im.wechat", f"/p{i}") for i in range(4)]
    for s in sessions:
        db.create_task(None, s.id, "x")
    pool = make_pool(db, fake_runner)                  # concurrency=2
    loop_task = asyncio.create_task(pool.run_forever())
    await asyncio.wait_for(fake_runner.started.wait(), 3)
    await asyncio.sleep(0.3)
    assert len(fake_runner.ran) == 2                   # 上限 2
    fake_runner.finish.set()
    loop_task.cancel()


async def test_cancel_pending_and_running(db, fake_runner):
    s = db.get_or_create_session("u@im.wechat", "/repo")
    db.create_task(None, s.id, "a")
    db.create_task(None, s.id, "b")
    pool = make_pool(db, fake_runner)
    loop_task = asyncio.create_task(pool.run_forever())
    await asyncio.wait_for(fake_runner.started.wait(), 3)
    await asyncio.sleep(0.1)
    assert "已取消" in await pool.cancel(2)             # pending → canceled
    assert db.get_task(2).state == "canceled"

    class FakeProc:
        def kill(self):
            fake_runner.finish.set()                    # kill 后 runner 返回
    fake_runner.procs[1] = FakeProc()
    reply = await pool.cancel(1)                        # running → kill
    assert "取消" in reply
    await asyncio.sleep(0.2)
    loop_task.cancel()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_pool.py -v`
Expected: FAIL

- [ ] **Step 3: 实现 `worker/pool.py`**

```python
"""任务调度池：按 session 分组串行、跨 session 并行、并发上限、取消。
同一 Claude 会话（同 UUID）任务必须串行（--resume 并发会冲突）——TRD 硬约束。"""
import asyncio

import common.models as M


class WorkerPool:
    def __init__(self, db, config, runner=None, concurrency: int = 3,
                 poll_interval_s: float = 0.5):
        self._db = db
        self._cfg = config
        if runner is None:
            from worker.runner import TaskRunner
            runner = TaskRunner(db, config, process_registry=self._procs)
        self._runner = runner
        self._concurrency = concurrency
        self._interval = poll_interval_s
        self._procs: dict[int, asyncio.subprocess.Process] = {}
        self._running_sessions: set[int] = set()
        self._wake = asyncio.Event()
        self._session_user = {}

    async def run_forever(self) -> None:
        while True:
            made = self._claim_one_round()
            if not made:
                try:
                    await asyncio.wait_for(self._wake.wait(), timeout=self._interval)
                except asyncio.TimeoutError:
                    pass
                self._wake.clear()

    def _claim_one_round(self) -> bool:
        running = len(self._running_sessions)
        free = self._concurrency - running
        started = False
        for sid in self._db.pending_sessions():
            if free <= 0:
                break
            if sid in self._running_sessions:
                continue
            task = self._db.claim_next_pending({sid})
            if task is None:
                continue
            session = self._db.get_session(sid)
            self._running_sessions.add(sid)
            started = True
            free -= 1
            asyncio.create_task(self._run_one(task, session))
        return started

    async def _run_one(self, task, session) -> None:
        try:
            await self._runner.run(task, session)
        except Exception as e:  # 保姆代码不允许崩掉整个池
            self._db.finish_task(task.id, "failed")
            self._db.audit("runner_crash", f"task={task.id} err={e!r}")
        finally:
            self._running_sessions.discard(task.session_id)
            self._wake.set()

    def running_session_ids(self) -> set[int]:
        return set(self._running_sessions)

    def snapshot(self) -> list[M.Task]:
        return self._db.active_tasks()

    async def cancel(self, task_id: int) -> str:
        task = self._db.get_task(task_id)
        if task is None:
            return "没有这个任务。"
        if task.state == "pending":
            self._db.cancel_task(task_id)
            return f"已取消任务 #{task_id}。"
        if task.state == "running":
            proc = self._procs.get(task_id)
            if proc is None:
                return f"任务 #{task_id} 正在运行但进程句柄未注册，稍后再试。"
            self._db.finish_task(task_id, "canceled")
            try:
                proc.kill()
            except ProcessLookupError:
                pass
            return f"已终止任务 #{task_id} 的进程。"
        return f"任务 #{task_id} 状态为 {task.state}，无需取消。"
```

（注：`__init__` 中 `self._procs` 需在 `runner is None` 分支前初始化——实现时把 `self._procs = {}` 移到方法第一行。）

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_pool.py -v`
Expected: 全部 passed

- [ ] **Step 5: Commit**

```bash
git add worker/ tests/ && git commit -m "feat: WorkerPool（session 串行/跨 session 并行/并发上限/取消）"
```

---

### Task 11: gateway/outbound.py — 出站发送器

**Files:**
- Create: `gateway/outbound.py`
- Test: `tests/test_outbound.py`

**Interfaces:**
- Consumes: ILinkClient、Database。
- Produces:
  - `common/text.py` 的 `split_text(text, limit) -> list[str]`（分页是出站职责，放 common 供 gateway 与 worker 共用，避免 worker→gateway 反向依赖）
  - `OutboundLoop(db, ilink, config)`：`await run_forever()`；`await push_now(item_ids)` 唤醒
  - `get_token() -> tuple[str, str]` 由构造参数传入（`token_ref: dict`，`{"token": ..., "base_url": ...}`，重连时原子替换——对齐 bot.py 的 bot_token_ref 模式）
  - typing 管理：构造参数 `typing_state: dict`（from_user → ticket 缓存）
  - config 需含：`throttle.min_send_interval_s`、`throttle.daily_send_limit`、`throttle.page_char_limit`

- [ ] **Step 1: 写失败测试 `tests/test_outbound.py`**

```python
import asyncio

import pytest

from gateway.outbound import OutboundLoop, split_text


class FakeILink:
    def __init__(self):
        self.sent = []
        self.fail_first = 0
        self.calls = 0

    async def sendmessage(self, to_user, context_token, text, token, base_url=None):
        self.calls += 1
        if self.calls <= self.fail_first:
            return False
        self.sent.append((to_user, context_token, text))
        return True

    async def getconfig(self, user, ctx, token, base_url=None):
        return "TICKET"

    async def sendtyping(self, user, ticket, status, token, base_url=None):
        self.sent_typed = (user, ticket, status)


class FakeCfg:
    def __init__(self):
        self.throttle = {"min_send_interval_s": 0.0, "page_char_limit": 2000,
                         "daily_send_limit": 500, "progress_window_s": 0.0}


def test_split_text():
    assert split_text("短文本", 2000) == ["短文本"]
    pages = split_text("x" * 5000, 2000)
    assert len(pages) == 3
    assert pages[0].startswith("(第 1/3 页)")


async def test_outbox_drain_marks_sent(db):
    il = FakeILink()
    db.insert_message(common_msg("u@im.wechat", "CTX-NEW"))
    loop = OutboundLoop(db, il, FakeCfg(),
                        token_ref={"token": "T", "base_url": ""},
                        typing_state={})
    db.enqueue(None, "u@im.wechat", "hello")
    task = asyncio.create_task(loop.run_forever())
    await asyncio.sleep(0.3)
    task.cancel()
    assert il.sent == [("u@im.wechat", "CTX-NEW", "hello")]   # 用最新 token
    assert db.get_outbox(1).state == "sent"


async def test_retry_then_success(db):
    il = FakeILink()
    il.fail_first = 2
    db.insert_message(common_msg("u@im.wechat", "CTX"))
    loop = OutboundLoop(db, il, FakeCfg(),
                        token_ref={"token": "T", "base_url": ""}, typing_state={})
    db.enqueue(None, "u@im.wechat", "m")
    task = asyncio.create_task(loop.run_forever())
    await asyncio.sleep(0.5)
    task.cancel()
    assert db.get_outbox(1).state == "sent"
    assert il.calls == 3


def common_msg(user, token):
    from common.models import InboundMessage
    return InboundMessage(msg_id="1", from_user=user, text="hi",
                          context_token=token, received_at=1)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_outbound.py -v`
Expected: FAIL

- [ ] **Step 3: 实现 `gateway/outbound.py`**

```python
"""出站发送器：outbox → iLink。最小发送间隔节流、失败重试、死信、每日上限熔断、typing 状态。"""
import asyncio
import random
import time

from common.text import split_text


class OutboundLoop:
    def __init__(self, db, ilink, config, token_ref: dict, typing_state: dict):
        self._db = db
        self._ilink = ilink
        self._cfg = config
        self._token_ref = token_ref          # {"token","base_url"}，重连方原子替换
        self._typing = typing_state          # from_user → typing_ticket
        self._wake = asyncio.Event()
        self._sent_today = 0
        self._day = time.localtime().tm_yday
        self._last_send = 0.0

    async def run_forever(self) -> None:
        while True:
            try:
                await self._drain_once()
            except Exception as e:
                import logging
                logging.getLogger(__name__).exception("outbound loop error: %s", e)
                await asyncio.sleep(1)
            try:
                await asyncio.wait_for(self._wake.wait(), timeout=0.5)
            except asyncio.TimeoutError:
                pass
            self._wake.clear()

    def notify(self) -> None:
        self._wake.set()

    async def _drain_once(self) -> None:
        today = time.localtime().tm_yday
        if today != self._day:
            self._day, self._sent_today = today, 0
        limit_cfg = self._cfg.throttle["daily_send_limit"]
        if self._sent_today >= limit_cfg:
            return  # 熔断：超每日上限暂停出站（/status 可见，明日自动恢复）

        for item in self._db.next_outbox_batch(limit=10):
            pages = split_text(item.text, self._cfg.throttle["page_char_limit"])
            ok = True
            for page in pages:
                await self._respect_interval()
                ok = await self._send(item.to_user, page)
            if ok:
                self._db.mark_sent(item.id)
                self._sent_today += 1
            else:
                self._db.mark_send_failed(item.id, "sendmessage 未确认")
                dead = self._db.dead_letter_count()
                if dead:
                    self._db.audit("dead_letter", f"count={dead} id={item.id}")

    async def _send(self, to_user: str, text: str) -> bool:
        token = self._token_ref["token"]
        base = self._token_ref["base_url"] or None
        try:
            await self._typing_on(to_user)
            ok = await self._ilink.sendmessage(
                to_user, self._db.latest_context_token(to_user) or "", text,
                token, base)
        except Exception:
            return False
        finally:
            try:
                await self._typing_off(to_user)
            except Exception:
                pass
        return ok

    async def _typing_on(self, user: str) -> None:
        if user not in self._typing:
            self._typing[user] = await self._ilink.getconfig(
                user, self._db.latest_context_token(user) or "",
                self._token_ref["token"], self._token_ref["base_url"] or None)
        ticket = self._typing.get(user)
        if ticket:
            await self._ilink.sendtyping(user, ticket, 1,
                                         self._token_ref["token"],
                                         self._token_ref["base_url"] or None)

    async def _typing_off(self, user: str) -> None:
        ticket = self._typing.get(user)
        if ticket:
            await self._ilink.sendtyping(user, ticket, 2,
                                         self._token_ref["token"],
                                         self._token_ref["base_url"] or None)

    async def _respect_interval(self) -> None:
        interval = self._cfg.throttle["min_send_interval_s"]
        wait = self._last_send + interval + random.uniform(0, 0.3) - time.monotonic()
        if wait > 0:
            await asyncio.sleep(wait)
        self._last_send = time.monotonic()
```

同时做两件配套修改：
1. 新建 `common/text.py`（`split_text` 的唯一定义，内容即上面删去的函数）；Task 9 中 `worker/runner.py` 顶部的本地 `split_text` 定义删除，改为 `from common.text import split_text`（DRY，单一实现）。
2. `tests/test_outbound.py` 的 `from gateway.outbound import OutboundLoop, split_text` 改为 `from common.text import split_text` + `from gateway.outbound import OutboundLoop`。

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_outbound.py tests/test_runner.py -v`
Expected: 全部 passed

- [ ] **Step 5: Commit**

```bash
git add gateway/ worker/ tests/ && git commit -m "feat: 出站发送器（outbox 投递/重试/死信/节流/typing/每日上限）"
```

---

### Task 12: gateway/bridge.py — 桥命令与 /help

**Files:**
- Create: `gateway/bridge.py`
- Test: `tests/test_bridge.py`

**Interfaces:**
- Consumes: Database、WorkerPool、router。
- Produces:
  - `async execute_bridge(db, pool, route, from_user, config) -> str`（回复文本）
  - `build_help(db, config) -> str`（三层合并：桥命令 + iLink + 已同步的 headless slash_commands）
  - `async execute_ilink_op(db, route, from_user, config, reconnect_fn) -> str`（/time /重新连接 /help）

- [ ] **Step 1: 写失败测试 `tests/test_bridge.py`**

```python
import json

import pytest

from gateway.bridge import build_help, execute_bridge
from gateway.router import Route


class FakeCfg:
    def __init__(self):
        self.reconnect = {"session_duration_s": 86400}
        self.default_cwd = "/repo"


class FakePool:
    def __init__(self, tasks):
        self._tasks = tasks

    def snapshot(self):
        return self._tasks

    async def cancel(self, task_id):
        return f"已取消任务 #{task_id}。"


def _route(cmd, args=""):
    return Route(kind="bridge", command=cmd, args=args, detail={})


async def test_cd_switches_session(db):
    db.get_or_create_session("u@im.wechat", "/old")
    db.set_active_cwd("u@im.wechat", "/old")
    reply = await execute_bridge(db, FakePool([]), _route("cd", "/new"), "u@im.wechat", FakeCfg())
    assert "/new" in reply
    assert db.get_active_cwd("u@im.wechat", "/d") == "/new"
    assert db.get_or_create_session("u@im.wechat", "/new").id != \
           db.get_or_create_session("u@im.wechat", "/old").id


async def test_policy_switch(db):
    s = db.get_or_create_session("u@im.wechat", "/repo")
    db.set_active_cwd("u@im.wechat", "/repo")
    reply = await execute_bridge(db, FakePool([]), _route("policy", "strict"), "u@im.wechat", FakeCfg())
    assert "strict" in reply
    assert db.get_session(s.id).policy == "strict"


async def test_policy_invalid(db):
    db.get_or_create_session("u@im.wechat", "/repo")
    reply = await execute_bridge(db, FakePool([]), _route("policy", "yolo"), "u@im.wechat", FakeCfg())
    assert "auto/strict/bypass/plan" in reply


async def test_tasks_listing(db):
    s = db.get_or_create_session("u@im.wechat", "/repo")
    db.create_task(None, s.id, "/review", kind="command")
    reply = await execute_bridge(db, FakePool(db.active_tasks()), _route("tasks"), "u@im.wechat", FakeCfg())
    assert "#1" in reply and "review" in reply


async def test_status(db):
    db.enqueue(None, "u", "x")
    reply = await execute_bridge(db, FakePool([]), _route("status"), "u@im.wechat", FakeCfg())
    assert "排队" in reply and "死信" in reply


async def test_cancel(db):
    reply = await execute_bridge(db, FakePool([]), _route("cancel", "3"), "u@im.wechat", FakeCfg())
    assert "3" in reply


def test_help_merges_three_layers(db):
    db.set_state("slash_commands", json.dumps(["review", "model"]))
    text = build_help(db)
    assert "/cancel" in text          # 桥命令层
    assert "/time" in text            # iLink 运维层
    assert "/review" in text          # headless 转发层
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_bridge.py -v`
Expected: FAIL

- [ ] **Step 3: 实现 `gateway/bridge.py`**

```python
"""桥命令（gateway 本地秒回）与 /help 生成。"""
import json
import time

BRIDGE_HELP = {
    "cancel": "/cancel <任务号> — 取消任务",
    "tasks": "/tasks — 查看 running/pending 任务",
    "status": "/status — 队列深度、死信数、当日费用、连接剩余",
    "cd": "/cd <目录> — 切换工作目录（=切换 Claude 会话）",
    "policy": "/policy <auto|strict|bypass|plan> — 权限档位",
}
ILINK_HELP = {
    "time": "/time — 连接剩余时间",
    "重新连接": "/重新连接 — 立即重新扫码连接",
}
POLICIES = ("auto", "strict", "bypass", "plan")


def _active_session(db, from_user: str, default_cwd: str):
    cwd = db.get_active_cwd(from_user, default_cwd)
    return db.get_or_create_session(from_user, cwd)


async def execute_bridge(db, pool, route, from_user: str, config) -> str:
    cmd = route.command
    if cmd == "cancel":
        if not route.args.strip().isdigit():
            return "用法：/cancel <任务号>（/tasks 查看）"
        return await pool.cancel(int(route.args.strip()))
    if cmd == "tasks":
        rows = pool.snapshot()
        if not rows:
            return "当前没有运行中或排队的任务。"
        lines = []
        for t in rows:
            mark = "▶️" if t.state == "running" else "⏳"
            lines.append(f"{mark} #{t.id} [{t.kind}] {t.prompt[:40]}")
        return "\n".join(lines)
    if cmd == "status":
        cost = db.today_cost_usd()
        remain = _remain_text(db, config)
        return (f"队列：{db.queue_depth()} 排队 / {len(pool.running_session_ids())} 会话运行中\n"
                f"死信：{db.dead_letter_count()}\n"
                f"当日费用：${cost:.2f}\n"
                f"连接剩余：{remain}")
    if cmd == "cd":
        path = route.args.strip()
        if not path:
            cwd = db.get_active_cwd(from_user, config.default_cwd)
            sessions = db.list_sessions(from_user)
            lines = [f"当前目录：{cwd}", "历史会话："]
            lines += [f"  · {s.cwd}" for s in sessions[:10]]
            return "\n".join(lines)
        import os
        if not os.path.isdir(path):
            return f"目录不存在：{path}"
        db.set_active_cwd(from_user, path)
        db.get_or_create_session(from_user, path)
        return f"已切换到 {path}（新目录 = 新 Claude 会话）"
    if cmd == "policy":
        arg = route.args.strip().lower()
        if not arg:
            s = _active_session(db, from_user, config.default_cwd)
            return f"当前档位：{s.policy}\n可切换：{'/'.join(POLICIES)}"
        if arg not in POLICIES:
            return f"无效档位 {arg}。可切换：auto/strict/bypass/plan"
        s = _active_session(db, from_user, config.default_cwd)
        db.set_policy(s.id, arg)
        db.audit("policy", f"user={from_user} session={s.id} → {arg}")
        return f"权限档位已切换为 {arg}（下一条消息生效）。"
    return f"未知桥命令 {cmd}"


def _remain_text(db, config) -> str:
    login_at = float(db.get_state("login_at") or 0)
    dur = config.reconnect.get("session_duration_s", 86400)
    remain = max(0.0, login_at + dur - time.time())
    h, m = int(remain // 3600), int(remain % 3600 // 60)
    return f"{h} 小时 {m} 分钟" if h else f"{m} 分钟"


def build_help(db) -> str:
    try:
        forwarded = json.loads(db.get_state("slash_commands") or "[]")
    except ValueError:
        forwarded = []
    lines = ["刀鱼可用命令（与 Claude Code CLI 同一套语法）：", ""]
    lines += [f"  {d}" for d in BRIDGE_HELP.values()]
    lines += [f"  {d}" for d in ILINK_HELP.values()]
    if forwarded:
        lines.append(f"  可转发给 Claude：{' '.join('/' + c for c in forwarded)}")
    lines.append("")
    lines.append("其余文本直接作为对话发给 Claude。")
    return "\n".join(lines)


async def execute_ilink_op(db, route, from_user: str, config, reconnect_fn) -> str:
    if route.command == "help":
        return build_help(db)
    if route.command == "time":
        return f"当前连接剩余时间：{_remain_text(db, config)}"
    if route.command == "重新连接":
        db.set_state("reconnect_confirm", from_user)
        return "确认立即重新连接？回复 Y 确认 / N 取消"
    return "未知运维命令"
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_bridge.py -v`
Expected: 全部 passed

- [ ] **Step 5: Commit**

```bash
git add gateway/ tests/ && git commit -m "feat: 桥命令执行器（cancel/tasks/status/cd/policy）+ 三层 /help"
```

---

### Task 13: common/config.py + gateway/app.py — 主入口组装

**Files:**
- Create: `common/config.py`、`gateway/app.py`
- Test: `tests/test_config.py`、`tests/test_e2e.py`

**Interfaces:**
- Consumes: 以上全部组件。
- Produces:
  - `load_config(repo_root: Path) -> Config`（dataclass：whitelist、default_cwd、claude_bin、throttle、budget→Budget、worker、reconnect、secrets、db_path、repo_root）
  - `gateway/app.py`: `async main_async()`、`start()`；入站管道 `async handle_inbound(db, cfg, pool, outbound, msg) -> None`
  - `gateway/login.py`: `async terminal_login(db, ilink) -> None`（扫码写 state）

- [ ] **Step 1: 写 `common/config.py`**

```python
"""配置加载：gateway/config.json（实例，gitignore）+ claude/secrets.env（gitignore）。"""
import json
import os
from dataclasses import dataclass, field
from pathlib import Path

from common.models import Budget


@dataclass
class Config:
    repo_root: Path
    db_path: Path
    whitelist: set[str]
    default_cwd: str
    claude_bin: str | list[str]
    throttle: dict
    worker: dict
    reconnect: dict
    budget: Budget
    secrets: dict = field(default_factory=dict)


def load_config(repo_root: Path | None = None) -> Config:
    root = repo_root or Path(__file__).resolve().parents[1]
    cfg_path = root / "gateway" / "config.json"
    if not cfg_path.is_file():
        raise SystemExit(f"缺少配置文件 {cfg_path}（参考 gateway/config.example.json）")
    raw = json.loads(cfg_path.read_text(encoding="utf-8"))

    secrets = {}
    env_path = root / "claude" / "secrets.env"
    if env_path.is_file():
        for line in env_path.read_text(encoding="utf-8").splitlines():
            line = line.strip()
            if line and not line.startswith("#") and "=" in line:
                k, v = line.split("=", 1)
                secrets[k.strip()] = v.strip()

    return Config(
        repo_root=root,
        db_path=root / "data" / "daoyu.db",
        whitelist=set(raw.get("whitelist", [])),
        default_cwd=raw.get("default_cwd", str(root)),
        claude_bin=raw.get("claude_bin", "claude"),
        throttle=raw.get("throttle", {}),
        worker=raw.get("worker", {"concurrency": 3, "poll_interval_s": 0.5}),
        reconnect=raw.get("reconnect", {}),
        budget=Budget(**raw.get("budget", {})),
        secrets=secrets,
    )
```

- [ ] **Step 2: 写 `gateway/login.py`（终端扫码）**

```python
"""终端扫码登录：渲染二维码、轮询确认、token 落盘。服务器首次部署/无人值守重连用。"""
import asyncio
import time

import qrcode


async def terminal_login(db, ilink, base_url: str | None = None) -> dict:
    local = [db.get_state("bot_token")] if db.get_state("bot_token") else []
    data = await ilink.get_bot_qrcode([t for t in local if t], base_url)
    qr_content = data.get("qrcode_img_content") or data["qrcode"]
    print("\n请用微信扫码登录（也可把此链接发到微信打开）：")
    print(qr_content)
    _render_qr(str(qr_content))

    current_base = base_url
    verify = None
    while True:
        result = await ilink.poll_login_status(data["qrcode"], verify)
        if result.get("bot_token"):
            db.set_state("bot_token", result["bot_token"])
            db.set_state("bot_base_url", result.get("baseurl") or "")
            db.set_state("login_at", str(time.time()))
            print("✅ 登录成功，token 已落盘")
            return result
        if result.get("already_connected"):
            print("服务端提示已连接，沿用当前 token")
            return {"bot_token": db.get_state("bot_token")}
        if result.get("expired") or result.get("verify_code_blocked"):
            data = await ilink.get_bot_qrcode([], current_base)
            print("二维码已过期，已重新生成：", data.get("qrcode_img_content") or data["qrcode"])
            _render_qr(str(data.get("qrcode_img_content") or data["qrcode"]))
        if result.get("redirect_base"):
            current_base = result["redirect_base"]
        if result.get("need_verifycode"):
            verify = input("请输入手机微信显示的数字配对码: ").strip()
        await asyncio.sleep(1)


def _render_qr(content: str) -> None:
    if not content.startswith("http"):
        return
    try:
        import urllib.request
        from PIL import Image
        req = urllib.request.Request(content, headers={"User-Agent": "Mozilla/5.0"})
        with urllib.request.urlopen(req, timeout=15) as resp:
            img = Image.open(__import__("io").BytesIO(resp.read())).convert("L")
        img = img.resize((min(72, img.width), max(1, img.height * min(72, img.width) // img.width)))
        for y in range(img.height):
            print("".join("██" if img.getpixel((x, y)) < 128 else "  "
                          for x in range(img.width)))
    except Exception:
        qr = qrcode.QRCode(border=1)
        qr.add_data(content)
        qr.make(fit=True)
        for row in qr.get_matrix():
            print("".join("██" if c else "  " for c in row))
```

- [ ] **Step 3: 写 E2E 测试 `tests/test_e2e.py`（fake iLink + fake claude 全链路）**

```python
"""E2E：入站微信消息 → 落盘去重 → 路由 → 任务池 → fake claude → outbox → fake iLink 发送。
验证 M1 验收标准中可自动化部分（真机微信验收另行手动做）。"""
import asyncio
import json
import sys
from pathlib import Path

import pytest

from common.db import Database
from gateway.app import handle_inbound
from worker.pool import WorkerPool

FIXTURES = Path(__file__).parent / "fixtures"


class FakeILink:
    def __init__(self):
        self.sent = []

    async def sendmessage(self, to_user, ctx, text, token, base_url=None):
        self.sent.append((to_user, text))
        return True

    async def getconfig(self, *a, **kw):
        return ""

    async def sendtyping(self, *a, **kw):
        pass


class FakeOutbound:
    def __init__(self, db, ilink):
        self.db, self.ilink = db, ilink

    def notify(self):
        pass  # E2E 直接同步 flush
    async def _drain_once(self):
        pass


class FakeCfg:
    def __init__(self, tmp_path, monkeypatch):
        self.repo_root = tmp_path
        self.whitelist = {"u@im.wechat"}
        self.default_cwd = str(tmp_path)
        self.claude_bin = [sys.executable, str(FIXTURES / "fake_claude.py")]
        self.secrets = {"ANTHROPIC_API_KEY": "sk"}
        self.throttle = {"progress_window_s": 0.0, "page_char_limit": 2000}
        from common.models import Budget
        self.budget = Budget()
        self.worker = {"concurrency": 2, "poll_interval_s": 0.01}
        self.reconnect = {"session_duration_s": 86400}
        monkeypatch.setenv("FAKE_CLAUDE_SCRIPT", str(FIXTURES / "review_stream.jsonl"))
        monkeypatch.setenv("FAKE_CLAUDE_STDIN_LOG", str(tmp_path / "stdin.log"))


def inbound(msg_id, text, token="CTX"):
    return {"message_id": msg_id, "seq": msg_id, "from_user_id": "u@im.wechat",
            "message_type": 1, "context_token": token,
            "item_list": [{"type": 1, "text_item": {"text": text}}]}


async def test_full_pipeline_chat_and_command(tmp_path, monkeypatch):
    cfg = FakeCfg(tmp_path, monkeypatch)
    db = Database(tmp_path / "e2e.db")
    db.ensure_schema()
    ilink = FakeILink()

    from worker.runner import TaskRunner
    runner = TaskRunner(db, cfg, process_registry={})
    pool = WorkerPool(db, cfg, runner=runner, concurrency=2, poll_interval_s=0.01)
    loop_task = asyncio.create_task(pool.run_forever())

    # 1) 普通文本 → chat 任务 + ACK
    await handle_inbound(db, cfg, pool, None, inbound(1, "你好"))
    acks = _texts(db)
    assert any("收到" in t for t in acks)
    # 2) /review → command 任务
    await handle_inbound(db, cfg, pool, None, inbound(2, "/review"))
    # 3) 去重：同 message_id 重投不产生新任务
    n_tasks = db.queue_depth() + 1
    await handle_inbound(db, cfg, pool, None, inbound(2, "/review"))
    assert db.queue_depth() + 1 == n_tasks
    # 4) 白名单外忽略
    outside = inbound(3, "hi"); outside["from_user_id"] = "stranger"
    await handle_inbound(db, cfg, pool, None, outside)
    # 5) 群消息忽略
    group = inbound(4, "hi"); group["group_id"] = "g1"
    await handle_inbound(db, cfg, pool, None, group)
    # 6) 等任务跑完
    await _wait_done(db, timeout=10)
    loop_task.cancel()
    texts = _texts(db)
    assert any("审查完成" in t for t in texts), texts


async def test_bridge_command_local_instant(tmp_path, monkeypatch):
    cfg = FakeCfg(tmp_path, monkeypatch)
    db = Database(tmp_path / "e2e.db"); db.ensure_schema()
    await handle_inbound(db, cfg, None, None, inbound(1, "/status"))
    texts = _texts(db)
    assert any("队列" in t for t in texts)
    assert db.queue_depth() == 0          # 桥命令不入队


def _texts(db):
    return [r["text"] for r in db._conn.execute("SELECT text FROM outbox")]


async def _wait_done(db, timeout):
    async def done():
        while True:
            states = [r["state"] for r in db._conn.execute(
                "SELECT state FROM tasks WHERE state IN ('pending','running')")]
            if not states:
                return True
            await asyncio.sleep(0.05)
    await asyncio.wait_for(done(), timeout)
```

- [ ] **Step 4: 跑测试确认失败**

Run: `python -m pytest tests/test_e2e.py -v`
Expected: FAIL（gateway.app 不存在）

- [ ] **Step 5: 实现 `gateway/app.py`**

```python
"""主入口：组装 gateway + worker，运行入站管道。gateway 永不阻塞、绝不等 Claude。"""
import asyncio
import json
import logging
import time
from pathlib import Path

import aiohttp

from common.config import load_config
from common.db import Database
from common.models import InboundMessage
from gateway.bridge import execute_bridge, execute_ilink_op
from gateway.ilink import ILinkClient
from gateway.outbound import OutboundLoop
from gateway.router import route
from gateway.reconnect import ReconnectTimer
from worker.pool import WorkerPool
from worker.runner import TaskRunner

log = logging.getLogger("daoyu")


async def handle_inbound(db, cfg, pool, outbound, msg: dict) -> None:
    """入站管道：类型过滤 → 白名单 → 落盘去重 → 路由 → 本地秒回或入队。"""
    if msg.get("message_type") != 1:
        return
    if msg.get("group_id"):
        return  # 群消息忽略（iLink 群聊未正式支持，防误回）
    from_user = msg.get("from_user_id", "")
    if from_user not in cfg.whitelist:
        log.info("非白名单用户 %s，忽略", from_user)
        return

    text = (msg.get("item_list") or [{}])[0].get("text_item", {}).get("text", "")
    msg_key = str(msg.get("message_id") or msg.get("seq") or "")
    if not msg_key:
        log.warning("消息缺 message_id/seq，跳过: %r", msg)
        return
    if db.insert_message(InboundMessage(
            msg_id=msg_key, from_user=from_user, text=text,
            context_token=msg.get("context_token", ""),
            received_at=int(time.time()))) is None:
        return  # msg_id 去重（iLink 重连后消息会重投）

    # 重连 Y/N 确认拦截
    pending_user = db.get_state("reconnect_confirm")
    if pending_user == from_user and text.strip().upper() in ("Y", "N"):
        db.set_state("reconnect_confirm", "")
        if text.strip().upper() == "Y":
            db.enqueue(None, from_user, "好的，正在重新连接…")
            db.set_state("reconnect_now", "1")
        else:
            db.enqueue(None, from_user, "已取消重新连接。")
        if outbound:
            outbound.notify()
        return

    try:
        slash = set(json.loads(db.get_state("slash_commands") or "[]"))
    except ValueError:
        slash = set()
    r = route(text, slash)

    if r.kind == "ilink":
        async def _noop():
            pass
        reply = await execute_ilink_op(db, r, from_user, cfg, _noop)
        db.enqueue(None, from_user, reply)
    elif r.kind == "bridge":
        reply = await execute_bridge(db, pool, r, from_user, cfg)
        db.enqueue(None, from_user, reply)
    elif r.kind == "unknown":
        sug = r.detail.get("suggestion")
        hint = f"未知命令 /{r.command}。" + (f"最接近：/{sug}" if sug else "发送 /help 查看可用命令")
        db.enqueue(None, from_user, hint)
    elif r.kind == "proxy":
        db.enqueue(None, from_user,
                   f"/{r.command} 是 TUI 交互命令，微信端代理版本将在 M2 提供。")
    else:  # chat / forward
        cwd = db.get_active_cwd(from_user, cfg.default_cwd)
        session = db.get_or_create_session(from_user, cwd)
        db.create_task(None, session.id,
                       text if r.kind == "chat" else f"/{r.command} {r.args}".strip(),
                       kind=r.kind)
        db.enqueue(None, from_user, "✅ 收到，处理中")
    if outbound:
        outbound.notify()


async def poll_loop(db, cfg, ilink, pool, outbound, token_ref) -> None:
    buf = db.get_state("get_updates_buf", "")
    while True:
        try:
            result = await ilink.getupdates(buf, token_ref["token"],
                                            token_ref["base_url"] or None)
        except Exception as e:
            log.warning("getupdates 失败，5s 后重试: %s", e)
            await asyncio.sleep(5)
            continue
        new_buf = result.get("get_updates_buf")
        if new_buf:
            buf = new_buf
            db.set_state("get_updates_buf", buf)
        for m in result.get("msgs") or []:
            try:
                await handle_inbound(db, cfg, pool, outbound, m)
            except Exception:
                log.exception("处理入站消息失败: %r", m)


async def main_async() -> None:
    logging.basicConfig(level=logging.INFO,
                        format="%(asctime)s %(levelname)s %(name)s %(message)s")
    cfg = load_config()
    db = Database(cfg.db_path)
    db.ensure_schema()
    cfg.db_path.parent.mkdir(parents=True, exist_ok=True)

    # ---- 崩溃恢复 ----
    n = db.reset_running_tasks()
    if n:
        log.info("崩溃恢复：重置 %d 个 running 任务为 pending", n)
        db.audit("recovery", f"reset_running={n}")
    if db.dead_letter_count():
        db.audit("startup_dead_letter", f"count={db.dead_letter_count()}")

    token = db.get_state("bot_token")
    base = db.get_state("bot_base_url", "")
    token_ref = {"token": token or "", "base_url": base}
    typing_state: dict = {}

    async with aiohttp.ClientSession() as session:
        ilink = ILinkClient(session)
        if not token:
            from gateway.login import terminal_login
            await terminal_login(db, ilink)

        TaskRunner_cfg = _runner_cfg(cfg)
        runner = TaskRunner(db, TaskRunner_cfg, process_registry={})
        pool = WorkerPool(db, cfg, runner=runner,
                          concurrency=cfg.worker.get("concurrency", 3),
                          poll_interval_s=cfg.worker.get("poll_interval_s", 0.5))
        outbound = OutboundLoop(db, ilink, cfg, token_ref, typing_state)

        tasks = [
            asyncio.create_task(pool.run_forever(), name="worker-pool"),
            asyncio.create_task(outbound.run_forever(), name="outbound"),
            asyncio.create_task(ReconnectTimer(db, cfg, ilink, token_ref,
                                               typing_state, outbound).run_forever(),
                                name="reconnect"),
            asyncio.create_task(poll_loop(db, cfg, ilink, pool, outbound, token_ref),
                                name="poll"),
        ]
        log.info("刀鱼已启动（gateway+worker 同进程）")
        await asyncio.gather(*tasks)


def _runner_cfg(cfg):
    """给 TaskRunner 的视图：补充 repo_root 定位的 mcp/settings 路径与分页配置。"""
    class _V:
        pass
    v = _V()
    v.claude_bin = cfg.claude_bin
    v.secrets = cfg.secrets
    v.repo_root = cfg.repo_root
    v.throttle = cfg.throttle
    v.budget = cfg.budget
    v.page_char_limit = cfg.throttle.get("page_char_limit", 2000)
    return v


def start() -> None:
    try:
        asyncio.run(main_async())
    except KeyboardInterrupt:
        pass


if __name__ == "__main__":
    start()
```

注意：`gateway/reconnect.py` 在此任务一并创建（`ReconnectTimer`）：定时检查剩余时间，到 `warning_before_s` 时经 outbox 提醒（写 `reconnect_confirm` state），剩 `force_before_s` 时置 `reconnect_now`；一个 watcher 协程发现 `reconnect_now` 后执行重连（调 `terminal_login` 同款流程拿新 token，原子替换 `token_ref`、清 `typing_state`、清 `reconnect_confirm`）。实现 ≤80 行，与 bot.py 的 `reconnect_timer_task` 行为对齐但走 DB/outbox。

- [ ] **Step 6: 跑 E2E 与全量测试**

Run: `python -m pytest -v`
Expected: 全部 passed

- [ ] **Step 7: Commit**

```bash
git add gateway/ common/ tests/ && git commit -m "feat: 主入口组装（入站管道/崩溃恢复/登录/token 落盘）+ E2E 全链路测试"
```

---

### Task 14: 文档与收尾

**Files:**
- Create: `README.md`
- Modify: `CLAUDE.md`（补充常用命令）

- [ ] **Step 1: 写 README.md**

内容包含：项目一句话介绍、首次部署步骤（venv → 安装 → `cp gateway/config.example.json gateway/config.json` 改白名单 → `cp claude/secrets.env.example claude/secrets.env` 填 key → `daoyu-login` 扫码 → systemd 启用）、常用命令表（桥命令/运维命令/转发）、M1 边界（strict 审批、--bg 长任务、MCP、媒体为 M2/M3）、测试命令 `python -m pytest`。

- [ ] **Step 2: 更新 CLAUDE.md**

在"当前状态"下补：M1 已实现（组件清单 + 行号级入口 [gateway/app.py](../gateway/app.py)）；"常用命令"节：`python -m pytest`（全量测试）、`python -m pytest tests/test_e2e.py`（E2E）、`daoyu-login`（扫码）、`python -m gateway.app`（前台运行）。

- [ ] **Step 3: 全量回归 + 手动验收清单**

Run: `python -m pytest -v`
Expected: 全部 passed

手动验收（需真机，部署到服务器后执行，逐项在微信里验证）：
1. 发"你好" → ≤2s 收到"✅ 收到，处理中" → 收到 Claude 回复
2. 发 `/review` → 收到 🔧 进度 → 收到完整审查报告（分页正常）
3. 连发两条消息 → 第二条排队（同 session 串行）
4. `/tasks` `/status` `/time` `/help` `/policy strict` `/cd` `/cancel`
5. `kill -9` 主进程 → systemctl restart → 任务自动恢复重跑、消息不丢
6. 同一消息重投（重启模拟）→ 去重不重复处理

- [ ] **Step 4: Commit**

```bash
git add README.md CLAUDE.md && git commit -m "docs: README 部署与使用说明 + CLAUDE.md 常用命令"
```

---

## Self-Review 结论

- **Spec 覆盖**：PRD M1 验收三条（多轮对话不丢消息 / /review 全流程 / 重启恢复）分别由 E2E + 手动清单覆盖；FR-1/2/3/8/9/10 M1 范围内全覆盖；FR-4 由 claude CLI 原生能力提供（转发即可）；FR-5/6/7 的 MCP、审批、配置代理按里程碑归 M2，计划内已注明。TRD §12 M1 六步（schema→收发→worker→命令总线→恢复→E2E）对应 Task 2-13。
- **已知偏差（如实登记，不擅自改 TRD）**：① 当前 CLI 无 `--permission-prompt-tool`（M2 审批需重选方案）；② `claude logs` 不存在，后台任务管理为 `claude agents --json`（M2）；③ `--permission-mode` 实际还有 `auto`/`manual`/`dontAsk` 档（M1 按原映射，`auto` 档实测后再评估）；④ 消息去重键实为 `message_id` 字段（TRD 写 `msg_id`，语义一致）；⑤ 新增 `state` 表存 bot_token/get_updates_buf/slash_commands（"一切先落盘"所需，TRD 五表之外）。
- **类型一致性**：跨任务接口已在 File Structure 节统一声明；Task 9 的 `task_user` 错误实现已在任务内标注修正方案（`_push(task, to_user, text)`）。
