# M3 余项 B：OCR MCP（RapidOCR 本地封装）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新建独立 stdio MCP server `worker/ocr_mcp.py`（工具 `ocr(path)`：本地 RapidOCR 识别图片文字），runner 合并层恒注入为系统条目 `daoyu-ocr`，Claude 在微信对话中可对图片做精确文字提取。

**Architecture:** 照 [worker/approval_mcp.py](../../worker/approval_mcp.py) 先例写 stdio JSON-RPC server，但无 DB / 任务 env 依赖（纯能力面工具）；引擎 lazy import（首次 tools/call 才加载）；runner `_write_daoyu_mcp_config` 在 daoyu 条目外恒注入 `daoyu-ocr` 条目（`sys.executable` + 绝对路径，不受 /mcp on/off 管辖）；`claude/settings.json` allow 放行工具引用。

**Tech Stack:** Python 3.11+ / rapidocr-onnxruntime 1.4.4（onnxruntime CPU 推理随传入）/ 既有 asyncio + pytest 测试设施。

## Global Constraints（spec 逐条，全程生效）

- server 键名 **`daoyu-ocr`**（连字符合法，与 mcp.json 的 `chrome-devtools` 同款形态）；工具名 **`ocr`**，inputSchema `{"path": {"type": "string"}}`、required `["path"]`。
- 工具描述文案：**"识别图片中的文字（本地 RapidOCR，中英混识，返回按行文本）"**。
- 返回：识别文本（多行 `\n` 连接，不带置信度）；失败返回 **`识别失败: <原因>`** 文本（读文件失败 / 非 PNG/JPEG / 未识别出文字）；意外异常走 tools/call 兜底 isError（text 同带 `识别失败: ` 前缀）。
- **lazy**：server 启动不加载引擎——`from rapidocr_onnxruntime import RapidOCR` 只在首次 tools/call(ocr) 路径的 `_get_engine()` 内发生。
- 输入白名单：复用 [gateway/media.py](../../gateway/media.py) `sniff_image`，再收紧只放行 **png/jpg**（GIF/WebP 动图非 OCR 合理输入）。
- 装配：**恒注入、不受 disabled 过滤**（注入发生在静态清单过滤之后）；`env` 为空 dict（无 DB / 任务依赖）；`command` = `sys.executable`，`args` = `<repo>/worker/ocr_mcp.py` 绝对路径。
- bg 任务无 MCP（M3 定论，不涉及）；`DAOYU_TOOLS` 常量**不动**（那是 daoyu 控制面 server 的工具集）。
- `claude/settings.json` `permissions.allow` 追加 **`mcp__daoyu-ocr__ocr`**（M3 教训：MCP 工具需 allow，headless 无确认通道）。
- `rapidocr-onnxruntime>=1.4` 进 pyproject `dependencies`（产品主功能，不做 optional）。
- /mcp 列表呈现为**系统条目**：`系统条目（恒装载）：daoyu（审批/发图）· daoyu-ocr（本地 OCR）`。

## 前置实测记录（2026-08-19，本机 Windows venv，rapidocr-onnxruntime 1.4.4）

写计划时已实测，实现者**无需重测**，直接按以下事实编码：

1. **API 形态**：`result, elapsed = RapidOCR()(bytes)`——`__call__` 签名收 `Union[str, numpy.ndarray, bytes, pathlib.Path]`，**bytes 直传可用**（无需临时文件中转）。返回 `result` 为 list，每行三元素 `[box(4点坐标), text(str), score(float)]`，**文本取 `row[1]`**；无识别结果时为 `None` 或空 list。
2. **模型随包分发**：pip 安装后离线直跑（init 0.78s + 首调 1.95s，零下载）——部署无需模型预热步骤。
3. **中英混识正确**：中文测试图识别为正确 Unicode（score 0.9986）；console 打印乱码是 Windows 管道编码层，server 内已 UTF-8 reconfigure + `json.dumps` 无此问题。

---

### Task 1: worker/ocr_mcp.py 独立 stdio server + 依赖

**Files:**
- Create: `worker/ocr_mcp.py`
- Modify: `pyproject.toml:6`（dependencies 一行）
- Test: `tests/test_ocr_mcp.py`（新建）

**Interfaces:**
- Consumes: `gateway.media.sniff_image(buf: bytes) -> str`（既有，返回 "png"/"jpg"/"gif"/"webp"，不认识则抛 MediaError）
- Produces: `worker.ocr_mcp` 模块；`_get_engine() -> RapidOCR 实例`（lazy，测试 monkeypatch 点）；`_ocr(args: dict) -> str`（工具实现，入参 `{"path": str}`）；`main()`（stdin 行循环）。Task 2 依赖：`worker/ocr_mcp.py` 文件存在（runner 注入 args 指向它）。

- [ ] **Step 1: 写失败测试**

创建 `tests/test_ocr_mcp.py`：

```python
"""worker/ocr_mcp.py 测试：函数级（fake engine）+ 子进程协议级（不触发引擎加载）。"""
import asyncio
import json
import subprocess
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]

from worker import ocr_mcp


class _FakeEngine:
    def __init__(self, rows):
        self._rows = rows
        self.calls = []          # 记录收到的是 bytes（直传契约）

    def __call__(self, data):
        self.calls.append(type(data))
        return self._rows, [0.1]


def _png(tmp_path) -> Path:
    """sniff_image 白名单内的最小 PNG 头 + 填充（不须是真图——引擎是 fake）。"""
    p = tmp_path / "a.png"
    p.write_bytes(b"\x89PNG\r\n\x1a\n" + b"\x00" * 16)
    return p


def test_ocr_joins_lines_without_score(monkeypatch, tmp_path):
    fake = _FakeEngine([[None, "文字A", 0.9], [None, "text B", 0.8]])
    monkeypatch.setattr(ocr_mcp, "_get_engine", lambda: fake)
    out = ocr_mcp._ocr({"path": str(_png(tmp_path))})
    assert out == "文字A\ntext B"
    assert fake.calls == [bytes]          # bytes 直传（实测 API 契约）


def test_ocr_missing_file(tmp_path):
    out = ocr_mcp._ocr({"path": str(tmp_path / "no.png")})
    assert out.startswith("识别失败: 读文件失败")


def test_ocr_rejects_gif(tmp_path):
    p = tmp_path / "a.gif"
    p.write_bytes(b"GIF89a" + b"\x00" * 16)
    out = ocr_mcp._ocr({"path": str(p)})
    assert out == "识别失败: 不支持的图片格式 gif（OCR 仅支持 PNG/JPEG）"


def test_ocr_no_text(monkeypatch, tmp_path):
    monkeypatch.setattr(ocr_mcp, "_get_engine", lambda: _FakeEngine([]))
    assert ocr_mcp._ocr({"path": str(_png(tmp_path))}) == "识别失败: 未识别出文字"


def test_ocr_lazy_import_server_module_only():
    """lazy 契约：import server 模块不加载 rapidocr（子进程隔离验证，防本进程污染）。"""
    code = (f"import sys; sys.path.insert(0, r'{ROOT}'); "
            "import worker.ocr_mcp; "
            "print('rapidocr_onnxruntime' in sys.modules)")
    out = subprocess.run([sys.executable, "-c", code],
                         capture_output=True, text=True, check=True)
    assert out.stdout.strip() == "False"


async def test_ocr_server_protocol_handshake():
    """子进程级 stdio JSON-RPC：initialize / tools/list / ping（不调 ocr，引擎零加载）。"""
    p = await asyncio.create_subprocess_exec(
        sys.executable, str(ROOT / "worker" / "ocr_mcp.py"),
        stdin=asyncio.subprocess.PIPE, stdout=asyncio.subprocess.PIPE,
        stderr=asyncio.subprocess.PIPE, cwd=str(ROOT))
    try:
        async def send(obj):
            p.stdin.write((json.dumps(obj) + "\n").encode("utf-8"))
            await p.stdin.drain()

        async def recv():
            line = await asyncio.wait_for(p.stdout.readline(), 10)
            assert line, "server 无响应或提前退出"
            return json.loads(line.decode("utf-8"))

        await send({"jsonrpc": "2.0", "id": 1, "method": "initialize"})
        resp = await recv()
        assert resp["id"] == 1
        assert resp["result"]["serverInfo"]["name"] == "daoyu-ocr"

        await send({"jsonrpc": "2.0", "id": 2, "method": "tools/list"})
        resp = await recv()
        tools = resp["result"]["tools"]
        assert [t["name"] for t in tools] == ["ocr"]
        assert tools[0]["inputSchema"]["required"] == ["path"]

        await send({"jsonrpc": "2.0", "id": 3, "method": "ping"})
        resp = await recv()
        assert resp["result"] == {}
    finally:
        p.kill()
        await p.wait()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/Scripts/python -m pytest tests/test_ocr_mcp.py -v`
Expected: FAIL，`ModuleNotFoundError: No module named 'worker.ocr_mcp'`（import 行报错）

- [ ] **Step 3: 写实现**

创建 `worker/ocr_mcp.py`：

```python
"""daoyu-ocr MCP server（stdio）：ocr（本地 RapidOCR 图片文字识别，中英混识）。

能力面工具（区别于 daoyu 控制面的审批/发图）——独立进程隔离，模型加载开销
只在真调 ocr 时发生（lazy import）。无 DB / 任务 env 依赖；runner 经临时 mcp
config 恒注入本 server（系统条目，不受 /mcp on/off 管辖）。
实测（2026-08-19，rapidocr-onnxruntime 1.4.4）：bytes 直传；result 每行
[box, text, score]，文本取 row[1]；模型随 pip 包分发（无下载步骤）。
"""
import json
import sys
from pathlib import Path

# 本 server 由 `python <repo>/worker/ocr_mcp.py` 拉起：sys.path[0] 是 worker/，
# _ocr 里 `from gateway.media import sniff_image` 需要 repo 根——自举，幂等
# （照 approval_mcp 先例）。
_REPO_ROOT = str(Path(__file__).resolve().parents[1])
if _REPO_ROOT not in sys.path:
    sys.path.insert(0, _REPO_ROOT)

_engine = None


def _get_engine():
    global _engine
    if _engine is None:
        from rapidocr_onnxruntime import RapidOCR   # lazy：首次调用才加载
        _engine = RapidOCR()
    return _engine


def _resp(id_, result):
    sys.stdout.write(json.dumps({"jsonrpc": "2.0", "id": id_, "result": result}) + "\n")
    sys.stdout.flush()


def _tools():
    return {"tools": [{
        "name": "ocr",
        "description": "识别图片中的文字（本地 RapidOCR，中英混识，返回按行文本）",
        "inputSchema": {"type": "object",
                        "properties": {"path": {"type": "string"}},
                        "required": ["path"]},
    }]}


def _ocr(args) -> str:
    from gateway.media import sniff_image
    path = str(args.get("path", ""))
    try:
        raw = Path(path).read_bytes()
    except OSError as e:
        return f"识别失败: 读文件失败 {e}"
    ext = sniff_image(raw)            # 白名单 PNG/JPEG/GIF/WebP（magic bytes）
    if ext not in ("png", "jpg"):     # 收紧：GIF/WebP 动图非 OCR 合理输入
        return f"识别失败: 不支持的图片格式 {ext}（OCR 仅支持 PNG/JPEG）"
    result, _elapsed = _get_engine()(raw)     # bytes 直传（实测支持）
    if not result:
        return "识别失败: 未识别出文字"
    return "\n".join(str(row[1]) for row in result)


def main():
    sys.stdin.reconfigure(encoding="utf-8")    # Windows 管道默认 cp936，JSON-RPC 必须 UTF-8
    sys.stdout.reconfigure(encoding="utf-8")
    for line in sys.stdin:
        line = line.strip()
        if not line:
            continue
        try:
            msg = json.loads(line)
        except ValueError:
            continue
        m = msg.get("method", "")
        if "id" not in msg:            # 通知（如 notifications/initialized）不回包
            continue
        if m == "initialize":
            _resp(msg["id"], {"protocolVersion": "2024-11-05",
                              "capabilities": {"tools": {}},
                              "serverInfo": {"name": "daoyu-ocr",
                                             "version": "0.1.0"}})
        elif m == "tools/list":
            _resp(msg["id"], _tools())
        elif m == "tools/call":
            name = (msg.get("params") or {}).get("name")
            args = (msg.get("params") or {}).get("arguments") or {}
            # 兜底（照 approval_mcp 先例）：磁盘满/引擎崩溃等不得击穿 server
            # 进程——返回 isError 文本（text 带「识别失败: 」前缀，Claude 可读）。
            try:
                if name == "ocr":
                    _resp(msg["id"], {"content": [{"type": "text",
                                                   "text": _ocr(args)}]})
                else:
                    _resp(msg["id"], {"content": [{"type": "text",
                                                   "text": "unknown tool"}],
                                      "isError": True})
            except Exception as e:
                _resp(msg["id"], {"content": [{"type": "text",
                                               "text": f"识别失败: {e!r}"}],
                                  "isError": True})
        elif m == "ping":
            _resp(msg["id"], {})
        else:
            _resp(msg["id"], {})


if __name__ == "__main__":
    main()
```

`pyproject.toml` 第 6 行：

```toml
dependencies = ["aiohttp>=3.9", "cryptography>=42", "rapidocr-onnxruntime>=1.4"]
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/Scripts/python -m pytest tests/test_ocr_mcp.py -v`
Expected: 7 passed（引擎零加载，全测试 <5s）

- [ ] **Step 5: Commit**

```bash
git add worker/ocr_mcp.py tests/test_ocr_mcp.py pyproject.toml
git commit -m "feat(m3-b): daoyu-ocr MCP server——本地 RapidOCR 图片文字识别（lazy 引擎、bytes 直传、PNG/JPEG 白名单）"
```

---

### Task 2: runner 恒注入 + settings allow + /mcp 列表系统条目

**Files:**
- Modify: `worker/cli_builder.py:33-34` 后（新常量）
- Modify: `worker/runner.py:366-380`（merged 注入段）与 `_write_daoyu_mcp_config` docstring
- Modify: `claude/settings.json`（allow 一行）
- Modify: `gateway/proxy.py` `_mcp` 列表两个分支
- Test: `tests/test_runner.py`（追加）、`tests/test_smoke.py:47` 附近（追加断言）、`tests/test_proxy.py`（追加）

**Interfaces:**
- Consumes: Task 1 的 `worker/ocr_mcp.py` 文件（runner args 指向它）；既有 `APPROVAL_MCP_SERVER`（cli_builder.py:33）
- Produces: `OCR_MCP_SERVER = "daoyu-ocr"`、`OCR_TOOL = "mcp__daoyu-ocr__ocr"`（cli_builder 常量，文档与测试引用）；runner 临时 mcp config 多一个恒注入条目

- [ ] **Step 1: 写失败测试**

`tests/test_runner.py` 末尾追加（照 `test_mcp_disabled_filtered_and_platform_expanded` 同款 fixture：`db, cfg, tmp_path, monkeypatch`、`FAKE_CLAUDE_ARGS_LOG` 快照）：

```python
async def test_mcp_config_includes_ocr_system_entry(
        db, cfg, tmp_path, monkeypatch):
    """余项 B：daoyu-ocr 系统条目恒注入（sys.executable + 绝对路径、env 空）；
    即使静态 mcp.json 的 disabled 误列同名也不受影响（注入在过滤之后）。"""
    args_log = tmp_path / "mcp_b1.log"
    monkeypatch.setenv("FAKE_CLAUDE_ARGS_LOG", str(args_log))
    claude_dir = cfg.repo_root / "claude"
    claude_dir.mkdir(parents=True, exist_ok=True)
    (claude_dir / "mcp.json").write_text(json.dumps({
        "mcpServers": {},
        "disabled": ["daoyu-ocr"],     # 误写同名：系统条目不受管辖
    }), encoding="utf-8")
    s = db.get_or_create_session("u@im.wechat", str(cfg.repo_root))
    t = db.create_task(None, s.id, "hi", kind="chat")
    runner = TaskRunner(db, cfg, process_registry={})
    await runner.run(db.get_task(t), s)

    assert db.get_task(t).state == "done"
    log = json.loads(args_log.read_text(encoding="utf-8"))
    servers = log["mcp_config"]["mcpServers"]
    ocr = servers["daoyu-ocr"]
    assert ocr["command"] == sys.executable            # 同解释器绝对路径
    assert ocr["args"] == [str(cfg.repo_root / "worker" / "ocr_mcp.py")]
    assert ocr["env"] == {}                            # 无 DB/任务 env 依赖
    assert "daoyu" in servers                          # 控制面条目照常
```

`tests/test_smoke.py:47` 现有断言 `assert "mcp__daoyu__send_image" in perms.get("allow", [])` 之后追加一行：

```python
    assert "mcp__daoyu-ocr__ocr" in perms.get("allow", [])
```

`tests/test_proxy.py` 追加（`_write_mcp` / `_route` / `FakeCfg` 为本文件既有 helper）：

```python
async def test_mcp_list_shows_system_entries(db, tmp_path):
    """余项 B：/mcp 列表呈现系统条目（恒装载，不受 on/off 管辖）。"""
    _write_mcp(tmp_path)
    reply = await execute_proxy(db, _route("mcp"), FakeCfg(tmp_path))
    assert "系统条目（恒装载）：daoyu（审批/发图）· daoyu-ocr（本地 OCR）" in reply


async def test_mcp_list_system_entries_when_no_static_servers(db, tmp_path):
    _write_mcp(tmp_path, {})
    reply = await execute_proxy(db, _route("mcp"), FakeCfg(tmp_path))
    assert "daoyu-ocr（本地 OCR）" in reply
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/Scripts/python -m pytest tests/test_runner.py::test_mcp_config_includes_ocr_system_entry tests/test_smoke.py tests/test_proxy.py::test_mcp_list_shows_system_entries -v`
Expected: FAIL（runner 测试 KeyError "daoyu-ocr"；smoke 断言 False；proxy 断言 False）

- [ ] **Step 3: 写实现**

`worker/cli_builder.py` 在 `APPROVAL_PROMPT_TOOL = ...`（第 34 行）之后追加：

```python
# 能力面 server 键（runner 临时 mcp config 恒注入，不受 /mcp on/off 管辖——
# 同 APPROVAL_MCP_SERVER 一样是系统条目，但无 DB/env 依赖）。
OCR_MCP_SERVER = "daoyu-ocr"
OCR_TOOL = f"mcp__{OCR_MCP_SERVER}__ocr"   # claude/settings.json allow 引用同一形态
```

`worker/runner.py`：import 行把 `APPROVAL_MCP_SERVER` 处扩为同时取 `OCR_MCP_SERVER`（第 13 行 `from worker.cli_builder import (...)` 列表内加 `OCR_MCP_SERVER`）；merged 注入段（现 366-380 行）改为：

```python
        merged = {"mcpServers": {
            **servers,
            APPROVAL_MCP_SERVER: {
                "type": "stdio",
                "command": sys.executable,
                "args": [str(Path(__file__).resolve().parent / "approval_mcp.py")],
                "env": {
                    "DAOYU_DB": os.path.abspath(self._db.path),
                    "DAOYU_TASK_ID": str(task.id),
                    "DAOYU_TO_USER": session.wechat_user,
                    "DAOYU_TOOLS": tools,
                },
            },
            # 余项 B：daoyu-ocr 能力面系统条目——恒注入（disabled 不管辖）、
            # 无 DB/env 依赖（ocr_mcp.py 自举 repo 根、引擎 lazy）。
            OCR_MCP_SERVER: {
                "type": "stdio",
                "command": sys.executable,
                "args": [str(Path(__file__).resolve().parent / "ocr_mcp.py")],
                "env": {},
            },
        }}
```

`_write_daoyu_mcp_config` docstring 首段补一句（「……合并 daoyu server 条目」后加「与 daoyu-ocr 能力面条目（恒注入，无 env）」）。

`claude/settings.json` allow 数组追加（保持既有项不动）：

```json
  "permissions": {
    "allow": [
      "mcp__daoyu__send_image",
      "mcp__daoyu-ocr__ocr"
    ],
```

`gateway/proxy.py` `_mcp`：空清单分支改为——

```python
    if not servers:
        return ("claude/mcp.json 中没有配置 MCP server。\n"
                "系统条目（恒装载）：daoyu（审批/发图）· daoyu-ocr（本地 OCR）")
```

列表分支 `lines.append(MCP_USAGE)` 之前插入一行——

```python
    lines.append("系统条目（恒装载）：daoyu（审批/发图）· daoyu-ocr（本地 OCR）")
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `.venv/Scripts/python -m pytest tests/test_runner.py tests/test_proxy.py tests/test_smoke.py -q`
Expected: 全 passed

Run: `.venv/Scripts/python -m pytest -q`
Expected: 全 passed（281 基线 + Task 1 的 7 + 本任务 3 ≈ 291，以实跑为准）

- [ ] **Step 5: Commit**

```bash
git add worker/cli_builder.py worker/runner.py claude/settings.json gateway/proxy.py tests/test_runner.py tests/test_smoke.py tests/test_proxy.py
git commit -m "feat(m3-b): runner 恒注入 daoyu-ocr 系统条目 + settings allow + /mcp 列表系统条目呈现"
```

---

### Task 3: 文档同步（PRD / TRD / CLAUDE.md / README）

**Files:**
- Modify: `docs/PRD.md`（FR-5 表）
- Modify: `docs/TRD.md`（§7 表、§7 持久级注、§11 两行）
- Modify: `CLAUDE.md`（当前状态、M2 清单、M3 清单、组件清单、实现顺序）
- Modify: `README.md`（M2 边界 OCR 行、worker 组件注释、测试计数）

**Interfaces:**
- Consumes: 前两任务的实现事实（daoyu-ocr 恒注入、allow、/mcp 呈现）
- Produces: 无（纯文档）

- [ ] **Step 1: 实跑全量测试记录总数**

Run: `.venv/Scripts/python -m pytest -q`
记录输出中的总数 N（约 291，**以下步骤中所有计数写这个 N，不写占位符**）。

- [ ] **Step 2: 改 PRD FR-5 表**

`docs/PRD.md` 现两行：

```markdown
| OCR（Tesseract 封装） | 从截图提取文字 |
| AI 视觉 | 看懂截图内容（补充 OCR 只能提文字的不足） |
```

改为（两行并一行；视觉理解由模型原生视觉承担，M3 实证 Read 看图）：

```markdown
| daoyu-ocr（RapidOCR 本地封装） | 从图片提取文字（中英混识，本地推理，runner 恒注入的系统条目） |
```

- [ ] **Step 3: 改 TRD 三处**

§7 表（现 197-204 行）两行：

```markdown
| tesseract-ocr | 截图文字提取（本地封装） | 默认 |
| ai-vision | 截图内容理解 | 默认 |
```

改为：

```markdown
| daoyu-ocr | 图片文字提取（RapidOCR 本地封装；runner 恒注入，不受 /mcp 启停管辖） | 系统（恒装载） |
```

§7 持久级变更段（207 行，余项 A 句后）追加一句：

```markdown
daoyu-ocr 为系统条目恒注入（2026-08-19 余项 B，spec `2026-08-19-ocr-mcp-design`）。
```

§11 两行——

现：`| OCR MCP 具体 server 选型/封装 | 待实现时定（Tesseract 本地封装为主） | M2 |`
改为：`| OCR MCP 具体 server 选型/封装 | 已落定：RapidOCR 本地封装（daoyu-ocr，rapidocr-onnxruntime 1.4.4，模型随包、bytes 直传） | 已实现（2026-08-19 余项 B） |`

现：`| ClawBot 媒体（CDN 加密上传） | 未实现 | 二期；一期以 OCR 文字 + AI 视觉描述替代 |`
改为：`| ClawBot 媒体（CDN 加密上传） | 已实现（M3 图片双向，真机验收 2026-08-19） | — |`（该行残留 M1 时代口径，顺带勘误）

- [ ] **Step 4: 改 CLAUDE.md 五处**

1. 首段「当前状态」：`253 个测试全绿` → `N 个测试全绿`（N = Step 1 实跑数；该行现为「281 个测试全绿」——以文件当前值为准替换为 N）；同段末尾「余下：OCR/视觉 MCP 选型（/mcp 启停与 /config 写入已完成，spec 2026-08-19-mcp-config-writable-design）」改为「M3 全部完成：媒体收发 + /mcp 启停与 /config 写入（余项 A，spec 2026-08-19-mcp-config-writable-design）+ OCR MCP（余项 B，spec 2026-08-19-ocr-mcp-design）」。
2. 「常用命令」节：`# 全量测试（281 个）` → `# 全量测试（N 个）`。
3. M2 功能清单「MCP 装载」行：`claude/mcp.json 已装 chrome-devtools / context7 / web-reader 三台（实测 connected；……）` 改为 `claude/mcp.json 已装 chrome-devtools / context7 / web-reader 三台（实测 connected；平台无关形态——command 直写 npx/uvx，runner 合并层 Windows 包 cmd /c（白名单 {npx,uvx}）+ 过滤 disabled，Linux 直传 + 冷缓存预热）；另有 daoyu-ocr 系统条目（RapidOCR 本地 OCR，runner 恒注入、不受 /mcp 启停管辖，余项 B）`。
4. M3 功能清单末尾「余下：OCR/视觉 MCP 选型……」行整行改为：`- **OCR MCP**：daoyu-ocr 独立 stdio server（[worker/ocr_mcp.py](worker/ocr_mcp.py)，工具 ocr(path)——本地 RapidOCR 中英混识、引擎 lazy、PNG/JPEG 白名单、bytes 直传实测 API）；runner 恒注入系统条目（disabled 不管辖）、settings allow `mcp__daoyu-ocr__ocr`；/mcp 列表呈现系统条目行。bg 无 MCP 口径不变。`（替换原「OCR/视觉 MCP 选型（…再评估）」整行）。
5. 组件清单 worker 节：`approval_mcp.py（daoyu MCP server：审批 approve + 发图 send_image）` 后追加 `、ocr_mcp.py（daoyu-ocr：本地 OCR）`。
6. 「实现顺序」节 M3 行「余下：OCR/视觉 MCP 选型（/mcp 启停与 /config 写入已完成，spec 2026-08-19-mcp-config-writable-design）」改为「余项 A（/mcp 启停 + /config 写入）与余项 B（daoyu-ocr）均已完成（2026-08-19），M3 闭环」。

（以上第 1-6 为六处，标题写五处以文件实际行文为准逐处替换，勿漏「常用命令」计数。）

- [ ] **Step 5: 改 README 三处**

1. 「开发」节：`# 全量测试（281 个）` → `# 全量测试（N 个）`。
2. 「M2 边界」节现整行：

```markdown
- **OCR / 视觉 MCP**（tesseract-ocr / ai-vision）：媒体入站打通后 Claude 用 Read 原生看图，按实际体验再评估；当前已装 chrome-devtools / context7 / web-reader 三台。
```

改为：

```markdown
- **OCR**：daoyu-ocr（RapidOCR 本地封装，中英混识）随任务恒装载（系统条目，不受 /mcp 启停管辖）；视觉理解由 Claude 模型原生视觉承担（Read 看图）。静态三台 chrome-devtools / context7 / web-reader 可经 /mcp 启停。
```

3. 「开发」目录树 worker/ 注释行 `approval_mcp daoyu MCP server（审批+发图，stdio）` 后追加 ` / ocr_mcp 本地 OCR（daoyu-ocr）`。

- [ ] **Step 6: 全量回归确认**

Run: `.venv/Scripts/python -m pytest -q`
Expected: N passed（与 Step 1 一致；文档不影响测试）

- [ ] **Step 7: Commit**

```bash
git add docs/PRD.md docs/TRD.md CLAUDE.md README.md
git commit -m "docs(m3-b): OCR MCP 落定口径四文档同步——PRD FR-5 表、TRD §7/§11、CLAUDE.md、README（含 ClawBot 媒体失实行顺带勘误）"
```

---

## 部署与真机验收（收尾后执行）

1. **生产服务器 部署**：
   - `ssh <user>@<server-ip>`（死代理坑：联网命令前 `unset http_proxy https_proxy`）。
   - venv 装依赖：`cd ~/proj/daoyu && .venv/bin/python -m pip install rapidocr-onnxruntime`（腾讯内网镜像已配则自动走；模型随包、无预热步骤）。
   - 本机 `git archive HEAD | ssh <user>@<server-ip> "tar x -C ~/proj/daoyu"` → 用户 `sudo systemctl restart daoyu`。
2. **微信验收**（spec §4 四项）：
   - 发一张含文字的图 + 「OCR 提取图中文字」→ 识别文字回微信（中文图 + 英文图各一张）；
   - `/mcp` 列表含「系统条目（恒装载）：… daoyu-ocr（本地 OCR）」行；
   - `/mcp off 1`（停任一静态条目）→ 列表确认 daoyu-ocr 系统行不受影响；
   - `/bg 测试任务` 回执仍明示无 MCP（bg 口径回归确认）。
3. 验收期实测登记（如与假设不符）：引擎首调时延微信侧体感、RapidOCR 对微信压缩图的识别质量。
